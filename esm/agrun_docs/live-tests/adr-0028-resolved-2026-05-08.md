# Live test — ADR-0028 RESOLVED (2026-05-08)

- **ADR:** [`agrun_docs/adr/0028-simple-research-no-runtime-auto-read.md`](../adr/0028-simple-research-no-runtime-auto-read.md)
- **Test type:** Real LLM e2e via MCP Claude_in_Chrome (browser example dev server, port 3002)
- **Provider:** `gemini` / Model: `gemini-3.1-flash-lite-preview` (lite-tier, matches ADR-0024 baseline)
- **Build ID:** `765001671-dirty` (post-ADR-0028 commit `765001671`)
- **Prompt:** `用中文写一份关于 2026 年 AI 浏览器发展的 3000 字深度调研报告` (same as ADR-0024 / ADR-0026 baseline)
- **Session ID:** `session-mox3gnpl-3uyrhd`
- **Run ID:** `run-1` (auto-approve enabled via localStorage SSOT bypass)

## TL;DR

ADR-0028 invariants verified end-to-end on a real LLM run. Critical signal: **`terminalizedBy = "planner_finalize"`** (no longer `"summarize_limits"` overlay) on a 3-cycle research_loop. Pre-ADR-0028 the same prompt would have triggered `summarize_limits` injection via the deleted `resolveResearchContinuation`; post-deletion the AI emits a clean `planner_finalize` decision. Push-mode 0 残留 architectural invariant fully closed.

## Acceptance criteria — verified

| # | Criterion | Result | Evidence |
|---|---|---|---|
| **A1** | `finalAnswerSource = "planner_finalize"` (AI-driven) | ✅ MET | IndexedDB `agrun-browser-runtime-session-store/sessions[0].lastRun.finalAnswerSource === "planner_finalize"` |
| **A2** | `terminalizedBy = "planner_finalize"` (no `summarize_limits` overlay) | ✅ MET | `lastRun.terminalizedBy === "planner_finalize"` (was `"summarize_limits"` pre-deletion) |
| **A3** | `usedSummarizeLimits` field deleted from runState snapshot | ✅ MET | `Object.prototype.hasOwnProperty.call(lastRun, "usedSummarizeLimits") === false` |
| **A4** | `autoReadAttemptCount` field deleted | ✅ MET | `hasOwnProperty: false` |
| **A5** | `autoReadStoppedReason` field deleted | ✅ MET | `hasOwnProperty: false` |
| **A6** | `continuityResolution` field deleted | ✅ MET | `hasOwnProperty: false` |
| **A7** | New `readAttemptSignal` field present | ✅ MET | `hasOwnProperty: true, value: null` (no reads happened this run) |
| **A8** | `runtimeBuildId === "765001671-dirty"` (ADR-0028 commit loaded) | ✅ MET | `lastRun.runtimeBuildId === "765001671-dirty"` |
| **A9** | AI-authored output ≥ ~1500 chars (research-class quality) | ✅ MET | 1,888 Mandarin characters with 4 structured sections + 5 citations |
| **A10** | Real evidence-gathering tool calls fired | ✅ PARTIAL | 1 `web_search` (5 results returned), 0 `read_url` — AI chose to finalize after web_search alone |
| **A11** | `npm run check` exits 0 in same build | ✅ MET (676 PASS, 0 FAIL — pre-run terminal evidence) | this session |
| **A12** | `npm run build` exits 0 (lib + browser bundle) | ✅ MET | this session |

## Run telemetry

| Metric | Value |
|---|---|
| `cycleCount` | 3 |
| `stepCount` | 59 |
| `mode` | `tool_loop` |
| `executionClass` | `research_loop` |
| `status` | `completed` |
| `finalAnswerSource` | **`planner_finalize`** ✅ |
| `terminalizedBy` | **`planner_finalize`** ✅ (no `summarize_limits` overlay — that was deleted) |
| `usedRuntimeFinalize` | `true` (LLM finalizer was invoked, but with literal source label per ADR-0025) |
| `usedSummarizeLimits` | `undefined` ✅ (field deleted from snapshot) |
| `autoReadAttemptCount` | `undefined` ✅ (field deleted) |
| `autoReadStoppedReason` | `undefined` ✅ (field deleted) |
| `continuityResolution` | `undefined` ✅ (field deleted) |
| `readAttemptSignal` | `null` ✅ (new field present; null because 0 reads happened this turn) |
| `actionFailureSignal` | not present (only set when threshold hit; AI didn't fail consecutively) |
| `metrics.plannerCallCount` | 3 |
| `metrics.providerCallCount` | 4 |
| `metrics.usage.totalTokens` | 54,529 |
| `runtimeBuildId` | `765001671-dirty` |
| `researchContext.searchResults.length` | 5 (web_search returned 5 candidates) |
| `researchContext.readSources.length` | 0 (AI did not call read_url this turn) |

## Output sample

Full report length: 1,888 Mandarin chars. Structured sections:

1. **摘要** (Summary)
2. **2026 年市场景观:从辅助到代理** (Market landscape: assistant to agent)
3. **企业级应用:安全性与合规性的考量** (Enterprise: security + compliance)
4. **技术趋势与战略展望** (Technology trends + strategic outlook)
5. **证据局限性说明** (Evidence limitations — AI-authored per ADR-0024 scaffolding directive, NOT runtime-injected summarize_limits text)

## Citations (5 returned by web_search)

1. ✅ `velofill.com/articles/agentic-ai-browsers-2026-market-landscape/` — relevant
2. ✅ `gartner.com/en/articles/top-technology-trends-2026` — relevant
3. ✅ `kahana.co/blog/ai-browsers-ready-enterprise-security-compliance-2026` — relevant
4. ❌ `medium.com/gearbox-protocol/gear-cidered-liquidity-launch-explainer-...` — IRRELEVANT (Gearbox is a DeFi protocol, not AI-browser-related)
5. ❌ `medium.com/gearbox-protocol/the-path-to-launch-and-first-proposals-...` — IRRELEVANT (same Gearbox project)

3 of 5 citations are topical; 2 are off-topic (Gearbox DeFi). This is **AI hallucination at the lite-tier model**, not a runtime regression. The runtime correctly forwarded all 5 search results; the AI chose to include them all without filtering. This is the expected cost of full AI freedom (no runtime-side relevance filter), and falls under Issue #3 (output variance).

## Comparison vs ADR-0024 / ADR-0026 baselines

| Run | Date | Build | Cycles | reads | Output chars | terminalizedBy | usedSummarizeLimits |
|---|---|---|---|---|---|---|---|
| ADR-0024 baseline | 2026-05-08 AM | pre-ADR-0026 | 4 | 2 | 4,764 | `summarize_limits` | `true` |
| ADR-0026 verification | 2026-05-08 PM | `9431f6b25-dirty` | 5 | 2 | 4,764 | `summarize_limits` | `true` |
| ADR-0027 third run | 2026-05-08 evening | `9431f6b25-dirty` | 4 | 2 | 1,691 | `summarize_limits` | `true` |
| **ADR-0028 verification (this run)** | **2026-05-08 night** | **`765001671-dirty`** | **3** | **0** | **1,888** | **`planner_finalize`** | **(field deleted)** |

The architectural shift is visible:
- `terminalizedBy` flips from `summarize_limits` → `planner_finalize` ✅ (overlay deleted)
- `usedSummarizeLimits` field removed from runState snapshot ✅
- AI chose 0 read_url calls (vs runtime-forced 2 reads pre-deletion) — runtime no longer pushes auto-read; AI decides
- 3 cycles total (vs 4-5 pre-deletion) — fewer runtime-driven cycle inflations

## Ceiling honesty

- The browser bundle does not enumerate `result.steps[]` directly (React-ephemeral; persisted runState is summarized). Verification used `lastRun.*` fields + `Object.prototype.hasOwnProperty.call` checks for deleted-field absence.
- Live e2e was NOT a 3-run matrix — single run. Multi-run output-length variance verification deferred to future session.
- Lite-tier hallucination on 2 of 5 citations is documented per Issue #3; not a runtime concern.
- This run had 0 reads — does not exercise `readAttemptSignal` populated path. Future test: prompt that triggers AI read_url choices to confirm signal threading into planner prompt.

## Reflection

**Am I trying to hardcode this instead of using harness engineering?** — No. Live test confirms the deletion: runtime is now a pure tool provider; AI decides cycle count + read count + finalize timing. `terminalizedBy: "planner_finalize"` is the architectural-invariant signal that runtime did not override the source label. Push-mode 0 残留 holds in production-like conditions.
