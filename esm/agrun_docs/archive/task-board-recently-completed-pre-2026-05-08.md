# Recently Completed — Archive (pre 2026-05-08)

Archived from `task.md` on 2026-05-14. Cutoff: 2026-05-08 (ADR-0023 ship date — push-mode 0 residue).

Rows in this section are historical verification evidence. They may mention runtime backstops, fallback compilers, or prompt-regex behavior that was valid for that slice.

| Key | Summary | Date |
|---|---|---|
| AGRUN-214m 3-topic Chrome MCP E2E acceptance | Closed AGRUN-214m by running the full 3-topic Chrome MCP live E2E quality gate against `?debug_yn=y&skill_provider=public&qa=agrun-214m-...-live-v1` on `gemini-3.1-flash-lite-preview`. (1) Company `TNO System PTE LTD`: 23 cycles, `final_with_limitations`, AGRUN-214n backstop synthesised `tno system pte ltd uen registry` query and reached companieshouse.sg (UEN 199700712E, incorporation 30 Jan 1997, SSIC 72102, registered address). (2) Person/handle `yapweijun1996`: 16 cycles, `final_with_limitations`, 2 strong sources (yapweijun1996.com + sg.linkedin.com), clean Profile Report with executive summary + professional identity + technical contributions + clickable sources. (3) Project `Vite frontend build tool`: 9 cycles, **full_report** mode, `authority:pass` (`official:yes independent:yes`), 3 strong sources (feature-sliced.design + vite.dev + v3.vitejs.dev), 20 supported / 0 unsupported claims, `evidence_quality_gate_passed`. All three runs produced clean H1 `Research Report: <topic>` titles with no internal OODAE/workspace/debug wording, all `researchState.topic` stayed entity-only (P4), and all coverageTargets used the new three-state coveredStatus (P3). Confirms the AGRUN-214m + AGRUN-214n + AGRUN-214o stack works across the company / person / project entity-type spectrum. | 2026-05-07 |
| AGRUN-214o Long Research Harness Hardening (P3/P4/P5/P6) | Four structural fixes from the AGRUN-214n debug-report review. (P3) `coverageTargets` carry a three-state `coveredStatus` (`covered`/`candidate_only`/`missing`), and `collectScopedReadSourceArtifacts` filters blocked-tier sources so Cloudflare titles never replace original page titles in user-facing Sources. (P4) `turnState.topicAnchorText` prefers the runtime-resolved `researchState.topic` and otherwise collapses verbatim research prompts via `extractEntityFromVerbatimPrompt` / `looksLikeVerbatimResearchPrompt`, so planner prompts no longer get the entire user request as `Current topic:`. (P5) Plan validation tags missing-`section.prompt` errors with `code: "missing_section_prompt"`; `executePlan` auto-strips `synthesize_per_action` for safe non-mutator plans and arms a one-shot "no plan envelope this turn" planner directive after repeated same-code failures. (P6) Finalize SSOT delivered indirectly via the AGRUN-214n veto backstop (forced `web_search` after `MAX_GUIDANCE_VETOES` keeps continuation + report-loop in agreement); a parallel snippet-salvage exit on the report-loop was prototyped and reverted because it broke approval-resume citation rendering — design documented inline. New unit tests `turn-state-topic-anchor.test`, `plan-validation-recovery.test` plus extensions to `research-report-loop.test`. Verified live via Chrome DevTools MCP `?debug_yn=y&skill_provider=public&qa=agrun-214o-live-v2` real LLM E2E (gemini-3.1-flash-lite-preview): full TNO research turn ran 8 OODAE cycles, AGRUN-214n backstop synthesised second `web_search` query `"tno system pte ltd uen registry"` after the planner failed to choose one, leading to companieshouse.sg primary record discovery. Final answer was a clean `Research Report: TNO System PTE LTD` with concrete verified facts (UEN 199700712E, incorporation 30 Jan 1997, SSIC 72102, registered address) plus a `Reported (Not Independently Verified)` section — no internal OODAE/workspace/debug wording, no cycle 9–18 dead loop. P3 coverage shows `official:yes registry:yes independent:no`; P4 `researchState.topic = "TNO System PTE LTD"` (not the 128-char verbatim prompt). Verification: targeted unit tests + `npm test` (55 suites green) + `npm run build` + `npm run dist:check` + browser lint + 10/10 browser smoke + Chrome MCP live LLM. | 2026-05-07 |
| AGRUN-214l Evidence Fallback Output Cleanup | During Chrome MCP company E2E, the minimal evidence-graph fallback leaked internal wording (`structured evidence gate`, AI final report availability) and repeated source-marked facts. The fallback compiler now uses user-facing limitation language and formats deduped source-backed findings after stripping inline source markers before re-attaching source ids. Regression coverage proves weak evidence fallback does not expose internal gate wording and does not duplicate repeated `(S1)` facts. Verification: targeted research-evidence-graph and research-report-loop unit tests passed; browser/root rebuilds completed for preview retest. | 2026-05-06 |
| AGRUN-214l Research Report Loop No-op Contract | Fixed the research report-loop harness contract so disabled or non-required gates are strict no-ops. `maybeCreateResearchReportLoopVeto()` now short-circuits disabled config and uses a side-effect-free `isResearchQualityGateRequired()` eligibility check before calling the evaluator that refreshes research state or materializes evidence graph data. Added regression tests proving `researchReportLoop`, `researchEvidenceGraph`, `researchWorkspace`, and `scopedEvidenceUrls` remain unchanged for `longResearchQualityGate: false` and for simple/non-long research. Verification: targeted unit test, root `npm run check`, browser lint, browser smoke, and browser build passed. | 2026-05-06 |
| AGRUN-215 OpenAI Responses API Provider Variant | Added backward-compatible OpenAI provider variant selection with `apiVariant: "responses"` while defaulting to Chat Completions. The request normalizer and runtime provider option merger now preserve `apiVariant`, `reasoningEffort`, and `reasoningSummary`; `openaiBrowserSkill` routes Responses requests through the AI SDK `openai.responses(model)`, forwards reasoning options via `providerOptions.openai`, supports `toolChoice: "none"`, and exposes `reasoningSummary` on provider output. Unit coverage pins request normalization and `/responses` transport/body behavior. | 2026-05-06 |
| AGRUN-214k AI-first Long Research Loop Hardening | Hardened the long-task harness without adding topic/domain hardcodes. Real progress is now a generic session marker advanced by successful search, read, TodoState, workspace, and skill-tool actions; repeated search skips do not count as progress and can converge to final-with-limitations finalization instead of looping. Coverage-search attempt accounting now increments only for actual planner-owned searches, including independent-source searches after runtime gap guidance. Source authority and source type checks share a generic domain-ownership SSOT, and multi-token topics use descriptor relevance so same-name but wrong-subject pages cannot become final findings. Final source lists are scoped to sources backing user-facing findings, while rejected/blocked/debug artifacts stay in Inspector. Verification: targeted unit/concern tests, 10-topic benchmark, root `npm test`, `npm run build`, `npm run dist:check`, browser lint/build, and browser smoke. | 2026-05-06 |
| AGRUN-214k AI-first Research Harness Boundary Cleanup | Removed the runtime full-report prose compiler path from long-research finalization. Runtime now materializes structured `researchEvidenceGraph` plus `researchFinalEnvelope` (`finalMode`, included/downgraded/omitted claim ids, `finalSourceIds`, evidence gaps), injects that gate into the AI finalizer prompt, and uses `claimGraph.include -> supportingSourceIds -> finalSourceIds` as the final citation SSOT. Removed fixed source-quality domain allowlists and brittle auto-read focus-token gating; planner/AI owns topic-specific source choice while runtime owns duplicate/read-budget/safety gates. Updated generic unit fixtures so TNO/Vite/handle live topics no longer drive unit behavior. Verification: `npm run build`, direct changed-unit tests, read-url/approval/session concerns, and full `npm test -- --runInBand` passed. | 2026-05-06 |
| AGRUN-214k AI-first Research Live E2E Hardening | Chrome MCP live E2E on `?debug_yn=y&skill_provider=public&qa_clean=y&qa_reset_settings=y&qa=tno-ai-first-research-live-v3` with natural prompt `Please research TNO System PTE LTD and give me a reliable public-source report.` verified the AI-first research gate after removing runtime search templates. Chrome Network showed one topic-specific SearXNG search and all external page reads going through `POST https://readurl.yapweijun1996.com/read-url` with no direct third-party browser fetches. Fixed two live findings: repeat-search blocking now runs before approval so duplicate search approvals are skipped, and source authority now requires registry-like source labels before promoting a source to primary. Silicon Review-style magazine profile pages are classified as contextual evidence, not primary/independent authority. The final report remains a `Research Report`, sanitizes internal claim-graph reason codes, and discloses the independent-authority gap. | 2026-05-06 |
| AGRUN-214k Chrome MCP E2E Read URL Finding | Chrome DevTools MCP live E2E on `?debug_yn=y&skill_provider=public&qa=agrun-214k-chrome-e2e-v1` with natural prompt `Please research TNO System PTE LTD and give me a reliable public-source report.` verified the research harness behavior but exposed an external source-acquisition blocker: the browser attempted `POST https://readurl.yapweijun1996.com/read-url`, but Chrome Network showed `net::ERR_FAILED` and console CORS preflight errors; shell checks also showed `/health`, `GET /read-url`, and `POST /read-url` returning Cloudflare 404 at the public hostname. The runtime correctly downgraded to `Research Report` with 0 usable observations, `Source assessment`, authority gap, Source Authority, OODAE timeline, and no random fallback citations. Follow-up: fix/readploy the read_url public endpoint or CORS contract before judging long research final quality from browser live runs. | 2026-05-06 |
| AGRUN-214k Read URL Proxy-Only Client Fix | After the remote read_url server engineer confirmed `https://readurl.yapweijun1996.com/read-url` now serves `/health` and CORS preflight correctly, fixed the browser client-side bug that retried failed read-url proxy requests by directly fetching the third-party target URL from the browser. `createBrowserRuntimeFetch()` now returns a structured `READ_URL_SERVICE_UNAVAILABLE` response instead of calling the external URL directly, preserving the proxy-only contract and avoiding misleading browser CORS failures. Added `examples/browser/test/read-url-fetch.smoke.ts` and wired it into browser smoke. Public POST is currently route/CORS-correct but local `.env.local` read_url key returns `API_KEY_INVALID`; live 200 E2E requires the current remote key. | 2026-05-06 |
| AGRUN-214k Read URL Valid-Key Live E2E | Updated ignored local `.env.local` with the current remote read_url key and restarted the browser dev server so Vite injected the new QA defaults. Chrome MCP live E2E on `?debug_yn=y&skill_provider=public&qa_clean=y&qa_reset_settings=y&qa=readurl-valid-key-live-v1` approved `read_url` for `https://example.com` and completed with `Example Domain`, success status `200`, source URL `https://example.com/`, and the expected sentence. Chrome Network showed only `POST https://readurl.yapweijun1996.com/read-url` returning `200` for the page read, with no direct browser `GET https://example.com`; Inspector classified the run as `Healthy Run` with `Read URL Status ok 200`. | 2026-05-06 |
| AGRUN-214k AI-first Research Move Gate | Removed the hardcoded long-research fallback plan/query path from `research-state` and `research-report-loop`. Before-finalize gates now emit planner guidance (`research_plan_required`, `research_next_step_required`, `research_evidence_gap_next_step`) instead of fabricating fixed `todo_plan` items or fixed `web_search` queries. The planner prompt now receives compact `researchReportLoop` state including source minimum, authority coverage, coverage targets, evidence gaps, recent searches, and zero-result queries. The action loop blocks repeated `web_search` queries already present in research history, forcing the agent to choose a fresh topic-specific query, read a different high-value source, or honestly finalize a final answer with explicit limitations. Verification: targeted research loop/state/finalizer tests, 10-topic benchmark, concern test, browser smoke, `npm run build`, `npm run dist:check`, and `npm test`. | 2026-05-06 |
| AGRUN-214k Live E2E Hardening | Live in-app browser QA on natural prompt `Please research TNO System PTE LTD and give me a reliable public-source report.` verified the v2 authority harness end-to-end. The run now recovers from empty Gemini planner responses with deterministic research fallback, emits a safe `Research Report` when read_url/search only produce thin or blocked source artifacts, preserves the user-facing `Source assessment` section through browser citation cleanup, and avoids appending random search-only fallback sources when the evidence graph has zero usable observations. Added a regression guard so non-research direct-final outputs still preserve `_Source: ...` provenance and `g3-followups` / `g3-drill-hints` UI extension blocks. | 2026-05-06 |
| AGRUN-214k Research Agent Protocol v2 | Upgraded long research from a source-count evidence compiler into a topic-driven authority harness. Runtime now resolves a generic `researchEntity`, scores every source with authority metadata (`primary`, `official`, `independent`, `context`, `non_verifying`, `blocked`), builds `claimGraph[]` decisions (`include`, `downgrade`, `omit`), and requires source-authority coverage before allowing a full `Research Report`. The report loop now reacts to missing authority coverage with targeted bounded search/read, while the end-user compiler outputs analyst-style sections: `Summary`, `Verified facts`, `Reported but not independently verified`, `Evidence gaps`, `Source assessment`, and `Sources`. Browser Inspector, Debug Report, and Support Bundle expose entity, source authority, authority coverage, and claim graph details. Benchmark coverage now scores authority coverage and unsafe verified claims in addition to source coverage, claim support, final quality, and noise leaks. | 2026-05-06 |
| AGRUN-214j Long Research Recovery + Evidence Cleanup | Fixed live natural-prompt QA where research turns could surface invalid planner envelopes, empty Gemini finalizer output, or low-value blocked/directory evidence. Planner recovery now treats search/research-style invalid envelopes as recoverable when `web_search` is available, and runtime finalization can compile a deterministic evidence-graph report when the provider returns empty finalizer output twice. Evidence admission now blocks Cloudflare/Just a moment challenge pages, RecordOwl-style company-profile directories, generic job-opening/career-opportunity lines, and unsupported draft placeholders from final observations, claim coverage, synthesis bullets, and final citations. Runtime finalize now emits the evidence-graph compiled report before the browser stores the assistant message, so end-user output includes `Source quality`, `Evidence gaps`, and `Claim coverage`. In-app browser E2E on the natural prompt "Please research TNO System PTE LTD and give me a reliable public-source report." passed with research timeline, no request failure, no invalid-envelope error, no empty-Gemini user error, no Cloudflare/Job Openings/unsupported-draft wording, and a properly evidence-constrained answer. | 2026-05-06 |
| AGRUN-214j Topic-driven Long Research Harness | Reviewed live Inspector data from `evidence-graph-synthesis-live-v17` and fixed the architecture gaps without hardcoding the QA topic. Natural end-user prompts such as "research this company and give me a reliable public-source report" now trigger the source-backed research harness; users do not need to say `long-run`, `todo_plan`, or `draft/critique/evaluate`. The harness requires a topic-specific `todo_plan` before complex finalization, derives coverage targets from the todo items and topic shape instead of a fixed official/repo/profile/independent template, uses company-like defaults (`official`, `registry`, `independent`) separately from handle/project defaults (`official`, `repo`, `independent`), and performs bounded targeted independent-source searches before limiting the report. Live in-app browser QA exposed a deeper evidence-admission bug where readable but unrelated pages such as Google Translate / Bubble sort could become final findings; the evidence graph now marks off-topic readable pages as `topic_mismatch`, blocks them from observations/claim evidence/citations, and sets an empty scoped citation set when no direct findings exist so unrelated fallback search links cannot be appended. Browser chat now has a sanitized end-user research progress timeline, Support issue classification no longer mislabels mixed successful/failed reads as a Tool/Read URL Issue when usable evidence exists, and a deterministic 10-topic benchmark pins source coverage, claim support, final quality, unsafe claim, and noise-leak rules. | 2026-05-06 |
| AGRUN-214i Evidence Graph Synthesis Quality Follow-up | Reworked the long-research evidence compiler after live in-app browser QA showed the v9/v10 output was safe but not intelligent: it dumped source identity logs, treated UI/app chrome and README implementation details as findings, and finalized too early after numeric source minimums. The OODAE report loop now continues when independent coverage is missing, skips low-value profile/social results when better unread candidates exist, and only treats coverage as present when a source produced usable non-identity observations. Final reports now use `Evidence-backed synthesis` plus `Reasoned interpretation`, normalize first-person owner-site claims into readable source-attributed statements, summarize GitHub evidence at project level, filter UI/README noise from synthesis and claim coverage, and size final source collection from scoped evidence URLs before display limits. In-app browser E2E `evidence-graph-synthesis-live-v17` on `yapweijun1996` passed: 3 approval-resumed search/read actions, `Research Report`, no UI chrome/README noise/source identity logs/false employment-education-company claims, 2 clickable final sources, source quality/evidence gaps/claim coverage present, and readable synthesis with reasoned limitations. | 2026-05-06 |
| AGRUN-214i Evidence Graph Report Compiler Live Fix | Reworked long-run research finalization around `runState.researchEvidenceGraph` so final reports are compiled from direct source observations instead of planner prose. The compiler now distinguishes source artifacts, observations, claim evidence, coverage, relevant-source minimums, and evidence gaps; social/profile-directory sources such as LinkedIn/Instagram are non-verifying for biography/employment/education claims; only sources with non-identity observations count as relevant final evidence; missing independent corroboration downgrades full reports to `Research Report`; final `Sources` are scoped to direct final findings. Added planner empty-response recovery for research turns so a recoverable empty Gemini planner response selects deterministic `web_search` fallback instead of failing the run. In-app browser E2E `evidence-graph-report-compiler-live-v9` on `yapweijun1996` passed: no TNO/LinkedIn/Instagram/education/employment false claims, two clickable final sources only, source quality states 4 read / 2 used for final findings, and required sections are present. | 2026-05-05 |
| AGRUN-214i Long Research OODAE Report Loop | Upgraded long-run research from final-answer repair into a runtime before-finalize OODAE report loop. `runState.researchReportLoop` now records Observe / Orient / Decide / Act / Evaluate cycles, enforces default source minimums (`minReadSources: 3`, `minRelevantSources: 2`), chooses `read_url` or `web_search` while evidence remains insufficient, and only allows `Research Report` when budget is exhausted before source minimum passes. Added `claimEvidence[]` for direct/partial/unverified claim support, with high-risk employment/company/education/profile/date/metric/project claims treated conservatively. Browser Inspector, Debug Report, and Support Bundle now expose the OODAE timeline and Claim Evidence table. | 2026-05-05 |
| AGRUN-214i Claim Coverage Live E2E Follow-up | In-app browser long-run research QA on `yapweijun1996` showed that adding `Claim coverage` was necessary but not sufficient: the final answer could still place weak employment/education/profile claims under supported findings. Added deterministic constrained-evidence claim cleanup so weak-source reports rename `What is supported` to `Evidence-constrained findings` and downgrade employment/company/education/profile-directory claims to not directly verified. Also aligned `researchWorkspace.claimCoverage` debug metadata with the final normalized answer so Inspector no longer reports `missing_claim_coverage_section` after the section is inserted. Live E2E confirmed planner-written virtual workspace artifacts (`planner-written:5`, `runtime-materialized:0`), `critique:pass`, Source Quality Detail, and Claim Coverage without missing-section false positives. | 2026-05-05 |
| AGRUN-214i Claim Coverage + Critique Gate | Tightened long-run research final synthesis after live QA showed a technically valid report could still overstate profile claims with only one source. Added runtime claim-coverage analysis for constrained-evidence research: missing `Claim coverage` now triggers final-response repair, deterministic final normalization inserts a conservative claim-coverage section before `Sources`, and `researchWorkspace.claimCoverage` is exposed to Inspector, Debug Report, and Support Bundle. `critique.md` is now a blocking virtual workspace artifact for long-run research/source-quality prompts, and workspace repair can generate a sanitized critique from research state. | 2026-05-05 |
| AGRUN-214i Virtual Workspace Quality Gate Follow-up | Live in-app browser E2E showed the technical flow worked but completed with weak workspace coverage: only `outline.md` and `final_candidate.md` were planner-written, while `evidence.json`, `draft.md`, and `critique.md` were missing and the final candidate looked like an outline. Tightened the runtime gate for long-run research/source-quality prompts so finalization is blocked until `evidence.json`, `draft.md`, `critique.md`, and `final_candidate.md` are populated and draft/final candidate are not merely `outline.md`. Browser Inspector now shows explicit Workspace Quality Warnings for non-pass checks such as `missing_evidence`, `missing_draft`, and `final_candidate_same_as_outline`. | 2026-05-05 |
| AGRUN-214i Inspector Source Quality Detail | Added sanitized source-quality explanations for read URL evidence. Runtime now exposes `explainReadSourceQuality()` with `tier`, `reason`, `signals`, and numeric metrics, stores the detail on `researchContext.readSources`, projects it into `researchWorkspace.sourceNotes`, and shows it in browser Inspector Evidence. Debug Report source lines now include reason/signals/text/overlap/status so QA can see why a URL was judged strong, medium/usable, weak, thin, or blocked without raw page text. In-app browser QA on `https://github.com/yapweijun1996` showed `strong`, `official_or_direct_strong`, `official_pattern`, `overlap:6`, `text:1125`, `status:200`, `origin:200`. | 2026-05-05 |
| AGRUN-214i Real Workspace Action Gate | Tightened complex-response finalization so long-run/research/report prompts must execute a real virtual workspace action before final/finalize. If the planner tries to finalize with no `write` / `replace` / `promote` / `workspace_finalize_candidate` operation, the before-finalize guard emits `missing_workspace_action`; repeated skips trigger a browser-safe `workspace_write` scaffold action instead of allowing immediate runtime materialization. Missing `final_candidate.md` bounded fallback now also returns a `workspace_write` action for the candidate, so materialization is no longer the first or only workspace process step. | 2026-05-05 |
| AGRUN-214i Inspector Workspace Origin Labels | Added explicit browser Inspector labels for virtual workspace artifact origin. The presenter now derives `planner-written` versus `runtime-materialized` from the workspace operation timeline, the Virtual Workspace panel shows the label on each file and latest operation, and Debug Report lines include the same origin field. This makes live QA clear when a final report came from planner workspace actions versus runtime materialization fallback. | 2026-05-05 |
| AGRUN-214i Long Research Source Quality Follow-up | Improved long-run handle/profile research source quality without hardcoding `yapweijun1996`. Weak-evidence retries now vary query shape: first retry asks for quoted official website/GitHub/LinkedIn/profile/portfolio evidence, and the next handle-like retry targets `site:github.com <topic>` instead of repeating the same broad query. Read-source quality now recognizes topic-owned first-party sources when a distinctive handle/brand token from the query matches the URL domain/path/title and extracted text is substantial; low-value marketplace/community sources are excluded. Updated `long-web-research` guidance to prefer first-party/profile/GitHub sources and vary weak-evidence retries. In-app browser E2E on `yapweijun1996` improved from `strong:0 weak:1 thin:1` to `strong:1 weak:0 thin:1`, with GitHub read as the strong source and remaining limitation correctly reported as `insufficient_relevant_sources`. | 2026-05-05 |
| AGRUN-214i Virtual Workspace Materialization Follow-up | Live in-app browser QA on long-run `yapweijun1996` research showed the planner could skip workspace actions after bounded evidence retries, leaving Inspector at `candidate:missing`. The runtime now materializes missing virtual workspace artifacts from sanitized `researchState`, `researchWorkspace`, and the final user-facing answer before finalization fallback: `outline.md`, `evidence.json`, `draft.md`, `critique.md`, and `final_candidate.md`. The operation timeline labels these as `materialize`, preserving the distinction between planner-written drafts and harness-projected artifacts. In-app browser E2E with `?debug_yn=y&skill_provider=public&qa_clean=y&qa_reset_settings=y` confirmed `Virtual Workspace candidate:ready`, all five files at `v1`, quality checks passing, and research state correctly finalizing with evidence limitations. | 2026-05-05 |
| Browser read_url In-app QA Reset | Added `qa_reset_settings=y` for browser QA URLs so `qa_clean=y` can optionally clear stale persisted settings and reload current `.env.local` defaults before a live turn. `loadSettings()` now backfills empty stored read-url endpoint/API-key fields from development defaults when available, while still respecting explicit `readUrl.enabled: false`. In-app browser E2E with `?debug_yn=y&skill_provider=public&qa_clean=y&qa_reset_settings=y` confirmed the approval path, `POST /read-url` service read, final `Example Domain` answer, and Inspector `Read URL Status ok 200 / origin 304`. | 2026-05-05 |
| AGRUN-214j Repo Tool Policy + Write Design | Tightened `repoFileTools` with `allowPaths` / `denyPaths` policy. The default denylist blocks `.env*`, secret/credential files, private-key names, and SSH identity filenames; optional allowlists constrain read/search to approved folders. Runtime action preflight, search output normalization, and the Node helper all enforce the policy. Added `agrun_docs/repo-file-write-tools-design.md` for future `repo_preview_patch` / `repo_apply_patch`: design-only, disabled unless host-injected, preview-before-apply, and approval bound to patch digest. | 2026-05-05 |
| AGRUN-214j Node Repo Adapter + Mobile Inspector Sheet | Added `node/repo-file-tools.cjs` as a reusable Node/server helper for `repoFileTools`, backed by `fs` reads and `rg --json` search under a configured `rootDir`. It keeps browser bundles clean because it is not imported by `src/index.js`; tests cover capped file reads, unsafe path rejection, search validation, and `rg` JSON parsing. Browser Inspector mobile UX now opens as a bottom sheet with backdrop close instead of a full-screen rail, while desktop stays a right rail. Chrome DevTools MCP transport remained closed in this environment, so live tab control is still blocked outside project code. | 2026-05-05 |
| AGRUN-214j Repo/File Read-only Tools + Inspector Workspace Split | Added optional `repoFileTools` host adapter support for real read-only project inspection. The runtime now exposes tier-1 `repo_rg` and `repo_read_file` only when the host provides `{ enabled: true, search/readFile }`; default browser/runtime behavior exposes no real file tools. Path/glob validation rejects absolute paths, parent segments, backslashes, empty segments, and output is capped before entering action history. Inspector UX now has a separate Virtual Workspace panel, keeping Evidence focused on sources/research instead of letting draft artifacts block context. | 2026-05-05 |
| AGRUN-214i Virtual Workspace Tools + Draft Quality Gate + Inspector | Added generic browser-safe `runState.virtualWorkspace` for complex final responses. The runtime now exposes tier-0 virtual workspace actions (`workspace_list`, `workspace_read`, `workspace_write`, `workspace_replace`, `workspace_finalize_candidate`) over fixed artifact files only: `outline.md`, `evidence.json`, `draft.md`, `critique.md`, and `final_candidate.md`. Path validation rejects absolute paths, `../`, backslashes, and unknown files; actions mutate only runtime state, never real files. Planner/finalizer prompts receive sanitized workspace artifact context, final-answer cleanup strips virtual workspace headings, approval resume preserves the workspace, and the browser Inspector/Debug Report/Support Bundle show files, operations, quality checks, and final-candidate readiness. Compatibility guardrail: ordinary short reports keep previous behavior; stricter final-candidate veto applies to long-run/research/source-quality prompts or when workspace drafting already started. | 2026-05-05 |
| AGRUN-214h Final Report Quality Guard | Tightened the long-run research final-answer boundary after live QA showed a report leaking internal workspace headings (`Research Workspace Progress`, `Evidence Notes`, `Gap Check`) and over-confident weak evidence. Added quality detection for `research_workspace_leak`, finalizer repair instructions, terminal output stripping for internal research workspace/progress sections, and docs/prompt contract that require long-run reports to separate `Summary`, `What is supported`, `Source quality`, `Evidence gaps`, and `Sources` without exposing the workspace scaffold. Weak/incomplete long-run research reports that skip source-quality or gap/limitation sections now trigger `missing_research_report_sections` repair, and terminal normalization has a deterministic fallback that inserts compact source-quality/gap sections from structured research metadata before `Sources:`. Browser example now normalizes final text from runtime output before storing/copying the assistant message, and final source collection filters unrelated single-topic read-url pages such as generic checker sites. | 2026-05-05 |
| AGRUN-214h Long-run Research Virtual Workspace | Added runtime `researchWorkspace` as a safe virtual workspace for long-run research. It records the research brief, plan questions, search log, source notes, evidence table, draft outline/synthesis notes, final-readiness state, and compact timeline. The finalizer receives the workspace as draft context but is instructed to output only the final user-facing report. Browser Inspector Evidence panel, Debug Report, and Support Bundle now expose workspace progress so QA can see the multi-step process without raw chain-of-thought. Approval resume now carries `researchWorkspace`, giving future resumable long-run flows a stable state surface. | 2026-05-05 |
| AGRUN-214g Long-run Research Quality Gate | Added runtime `researchState` as the SSOT for long-run topic research quality: phase, topic, source-quality counts, evidence gaps, bounded veto count, `finalAllowed`, and `finalReason`. The before-finalize path now blocks weak/thin-only `long-web-research` reports for a bounded retry and issues a follow-up `web_search` for stronger primary/official sources before allowing finalization with limitations. Browser Inspector Summary/Evidence, Debug Report, and Support Bundle now expose research phase, evidence quality, gaps, and finalization reason. Unit and browser smoke coverage pin gate behavior and debug projection. | 2026-05-05 |
| AGRUN-214g Long-run Research Source Relevance Follow-up | Live browser QA on topic `yapweijun1996` proved the long-run research harness can rank `long-web-research` Top-1, execute approval/resume research actions, and produce a final report, but initially appended an unrelated `Gemini Definition | Astrology Answers` source when direct `read_url` evidence was thin/weak. Added a single-topic source relevance guard in final `Sources:` fallback selection: distinctive prompt terms such as quoted handles must match candidate title/snippet/content/URL before search results can become final citations. Regression coverage pins that unrelated single-topic fallback sources are dropped while matching profile/source URLs survive. Re-run live QA no longer showed the astrology source; residual risk is that read_url source quality can still be thin/weak depending on public pages. | 2026-05-05 |
| AGRUN-214g Mobile Inspector Sticky Support Follow-up | Fixed the mobile Inspector debug-state regression where the large Support card stayed `sticky top-0` inside the full-screen overlay and blocked later context such as Skill Ranking. Sticky Support is now desktop-only; on mobile, the Support card scrolls naturally with the Inspector content while the overlay header and close button remain available. Docs record the mobile/desktop split. | 2026-05-05 |
| AGRUN-214g Mobile Inspector + Long Web Research Follow-up | Browser Inspector no-debug UX now avoids the mobile split-pane failure: on small viewports the Inspector opens as an overlay, the close control has an accessible label, and missing debug snapshots show one compact Developer Inspector empty state while detailed Support/Skill Ranking/Diagnosis/Evidence/Raw panels stay hidden until debug data exists. Added bundled `long-web-research` SKILL.md plus regenerated repo and browser-public manifests so Top-K can select a long-running topic research contract for plan -> search/read -> evidence -> gap check -> final report workflows. Docs now distinguish quick `web-research` from long-run research. | 2026-05-04 |
| AGRUN-214g Review Follow-up | Closed review findings from the browser static skill provider slice. `skill-provider-public.smoke.ts` now tests the pure `browser-skill-provider` helper instead of importing `agent.ts` and crossing the Rollup/Vite-only `virtual:agrun-agent-skills` boundary under `tsx`; the fallback case now exercises a failed manifest load. `usage-quickstart.md` now documents `plannerMode: "auto"` as the default, and the generated `dist/agrun.md` / `dist/agrun_docs/usage-quickstart.md` were rebuilt. Verification: browser smoke, browser lint, browser build, skills generator/copy unit checks, and root `npm run check`. | 2026-05-04 |
| AGRUN-214g Browser Static Skill Provider + Generator Warnings | Browser example now has an opt-in public skill provider QA path via `?skill_provider=public` or `localStorage['agrun.browser.skills.provider']='public'`. It loads `/skills/manifest.json` with `loadSkillIndexProvider()` before runtime creation, falls back to bundled skills on missing/empty manifests, and exposes sanitized `skillProviderMode`, `skillProviderStatus`, and `skillProviderManifestUrl` debug config. The skills generator now returns and CLI-prints non-fatal warnings for malformed/missing `toolSummaries`; copy helper propagates the same warnings. Browser smoke and unit tests pin loaded/fallback behavior, no setup-time `SKILL.md` fetch, warning output, and compatibility. | 2026-05-04 |
| AGRUN-214g Browser Public Skills Copy Helper | Added `build/copy-browser-skills.cjs` and `npm run skills:copy:browser` to clean-copy repo `skills/` into `examples/browser/public/skills/` for Vite static serving, skipping `.DS_Store`, `node_modules`, and source `manifest.json`. The helper regenerates the target `manifest.json` with existing generator logic so browser code can load `/skills/manifest.json`. Unit coverage pins static copy, stale cleanup, nested skill paths, skipped artifacts, CLI `--root`, and validation failures. | 2026-05-02 |
| AGRUN-214g Browser Inspector Skill Metadata View | Browser Inspector now projects selected skill metadata from `runtime.skillCatalogRanking.matches[]`. The Skill Ranking panel shows compact `category`, `namespace`, `tool`, and tool `tier` chips; Debug Report match lines include category/namespace/tool names; Support Bundle `userReport` adds `skillCatalogToolCount` and `skillCatalogCategories`. Browser smoke pins presenter, Debug Report, and Support Bundle output. | 2026-05-02 |
| AGRUN-214g Tools Metadata in Skills Manifest | Added explicit `toolSummaries` support for `tools.mjs`. The skills manifest generator now reads sanitized non-executable tool metadata into `manifest.tools[]` while preserving `toolsPath` as the lazy executable boundary. Missing or malformed summaries keep `tools: []`; accidental `func` values are stripped. `worldtime_tz/tools.mjs` now exports `toolSummaries`, and regenerated `skills/manifest.json` exposes `worldtime_now` metadata without an executable function. | 2026-05-02 |
| AGRUN-214g Nested Category Skills Manifest | Extended `build/generate-skills-index.cjs` from direct `skills/*/SKILL.md` scanning to recursive `skills/**/SKILL.md` scanning. Direct skills keep legacy `skillId === name`; nested skills get `category` from the first folder segment, `namespace` from parent folder path joined by `.`, and default `skillId` as `${namespace}.${name}` unless frontmatter `skillId` overrides it. Runtime manifest normalization and lazy-load merge now preserve `category` and `namespace`. Unit coverage pins nested paths, custom output relative paths, explicit skill id override, duplicate generated ids, and provider metadata preservation. | 2026-05-02 |
| AGRUN-214g Configurable Skills Generator Dirs | Extended `build/generate-skills-index.cjs` with `--skills-dir` and `--out` so static/browser publish folders can generate manifests outside the repo-root `skills/` path. Default `npm run skills:index` behavior is unchanged. Generated `sourcePath` and `toolsPath` are now relative to the output manifest location, matching `loadSkillIndexProvider(manifestUrl)` lazy URL resolution. Unit coverage pins helper options, CLI success, missing values, unknown args, invalid skills dirs, and missing output parent failures. | 2026-05-02 |
| AGRUN-214g Skills Manifest Generator | Added `build/generate-skills-index.cjs` and `npm run skills:index` to deterministically generate `skills/manifest.json` from repo `skills/*/SKILL.md`. The generated manifest contains summaries only, never `instructions`, includes `sourcePath`, optional `toolsPath`, empty v1 `tools`, stable SHA-256 checksum over `SKILL.md` plus `tools.mjs`, and fails on missing name/description, duplicate skill id, or empty skill directory. Unit coverage pins deterministic output, checksum invalidation, and validation failures. | 2026-05-02 |
| AGRUN-214f Browser Inspector Ranking Health | Browser Inspector now derives a presentation-only ranking health view from `runtime.skillCatalogRanking`. Summary, Support, Skill Ranking panel, Debug Report, and Support Bundle show whether Top-K prompt guard is active, selected/total candidate ratio, policy-filter count, and health status (`ok`, `warning`, `issue`). Browser smoke covers ok, over-Top-K issue, no-match warning, and no-ranking cases. | 2026-05-02 |
| AGRUN-214e Skill Catalog Benchmark Harness | Added deterministic 1000-skill fake catalog fixtures with controlled names, tags, duplicate tool names, risk tiers, requirements, versions, and checksums. New scale benchmark covers lexical Top-K ranking, provider `listManifests()` / `getManifest()` hot paths, policy/availability filtering, custom ranker hard-cap behavior, and duplicate `toolName` lookup requiring explicit skill id/name. Planner concern now reuses the same fixture and asserts only Top-K candidates enter the prompt. | 2026-05-02 |
| AGRUN-214d Browser Inspector Policy Metadata | Browser Inspector now projects `skillCatalogRanking.policyFilteredCount` and sanitized `policyFilteredReasons` into Summary, Support, Skill Ranking panel, Debug Report, and Support Bundle. QA can see which skills were filtered by explicit policy, missing features, input type mismatch, or risk tier without opening raw JSON. Browser smoke pins Summary, Debug Report, and Support Bundle fields. | 2026-05-02 |
| AGRUN-214d Skill Policy Gate | Added independent `skillPolicy` for skill/tool `allow` / `ask` / `deny`, manifest `riskTier`, `requires`, and availability metadata. Denied/unavailable manifests are filtered before Top-K and `list_agent_skills`; explicit read/use/execute attempts fail closed; ask-gated `execute_skill_tool` reuses the existing approval resume flow. Runtime events now emit sanitized `skill-policy-filtered`, `skill-policy-denied`, and `skill-policy-approval-required` details, and docs describe the public contract. | 2026-05-01 |
| AGRUN-214c Lazy Skill Index Provider | Added `loadSkillIndexProvider(manifestUrl)` as the scalable one-folder skill catalog path. It fetches only `manifest.json` at setup, serves manifest summaries through `listManifests()` / `getManifest()`, lazy-loads exactly the selected `SKILL.md` and optional `toolsPath` module through `loadSkill()`, caches by `skillId + version + checksum + sourcePath + toolsPath`, and invalidates after `refreshManifests()` observes version/checksum changes. `loadAgentSkills()` remains eager and backward compatible. Docs now describe the single static skills folder deployment shape. | 2026-05-01 |
| Browser Read URL Inspector Hardening | Fixed Browser Inspector diagnosis so completed runs with successful `read_url` sources are classified as `Healthy Run` instead of `Tool/Read URL Issue`. Thin/weak successful reads now show as a low-detail warning rather than an error, the Support card exposes `Read URL Status` as a compact state chip such as `ok 200`, `error 401`, or red `error`, Debug Report `[read_urls]` includes `status` plus `status_tone`, and Support Bundle JSON exposes `essentials.readUrlStatus` for machine-readable handoff. Added smoke coverage for successful-thin, invalid-key, and invalid-endpoint cases plus sequential live CDP command `npm --prefix examples/browser run test:live:read-url:all`, which verifies Chrome Network `POST https://readurl.yapweijun1996.com/read-url` returns 200/401 and an intentionally wrong endpoint returns 404 without local port contention. The release checklist now calls this all-in-one command out as the required local smoke for browser `read_url` / Inspector source-diagnosis changes, and the Web UI integration contract documents the read-url Debug Report and Support Bundle status/tone fields. | 2026-05-01 |
| Read URL Finalizer Target Extraction Fix | Fixed the read-url live-test regression where a simple `read_url` request (`Read https://example.com ... Return title/status/source`) was rewritten into `Evidence is insufficient to provide a verified current news report...`. Root cause: the research target extractor used capitalized command words such as `Read` and `Return` as fallback multi-target entities for source/URL prompts. The fallback proper-noun extractor now only runs for news/headline/current-events prompts; single-URL source reads still use read evidence and final answer normalization but do not trigger multi-target news coverage. Public endpoint `https://readurl.yapweijun1996.com/read-url` verified 200 with key; browser clean live test now returns `Example Domain`, success status, source URL, and page sentence. | 2026-05-01 |
| Browser Web Research Skill | Added bundled `web-research` SKILL.md so planner Top-K can select a source-grounded research instruction package for search, URL/article review, and current-info tasks. The skill documents the correct read-url service contract (`POST /read-url`, configured endpoint/key, no guessed `/read`/`/fetch` paths, no key exposure). Bundled skill generation and `parseSkillMarkdown()` now preserve `tags` and `inputTypes`, improving AGRUN-214b ranking metadata. Follow-up docs and MCP KB now record the browser read-url adapter contract for future QA and support handoff. | 2026-04-30 |
| AGRUN-214b Browser Inspector Ranking Metadata | Browser example now carries `runState.skillCatalogRanking` into `MessageDebugSnapshot`, shows Skill Top-K in the sticky Support card and Summary model, adds a dedicated Inspector Skill Ranking panel with selected/total/topK/filtered counts plus score and matched field chips, and includes the same metadata in Debug Report and Support Bundle for QA handoff. Browser smoke pins Summary/Report/Support Bundle output. | 2026-04-30 |
| AGRUN-213r Terminal Envelope Policy | Added a shared terminal envelope policy for `buildEnvelopeLines()`. Normal contexts still expose both `type:"finalize"` and `type:"final"` so runtime-finalize evidence synthesis remains available. The high-risk context `effectivePlannerMode:"envelope"` + Gemini lite request + unfinished TodoState now omits `finalize`, keeps direct `final`, reuses the same policy in repair-cascade and strict retry prompts, and rejects stray `finalize` envelopes before execution. Updated planner architecture/native-tools docs and added unit coverage for filtered and non-filtered contexts. | 2026-04-29 |
| AGRUN-213q Planner Mode Auto Resolver | Changed `plannerMode` default from host-facing `"native_tools"` to `"auto"`. agrun now resolves effective planner mode through `src/runtime/provider-capabilities.js` using provider/model/tool-surface rules: Gemini lite models with complex native plan surfaces such as `execute_skill_tool` use envelope, explicit overrides remain honored, and other supported surfaces prefer native tools. Runtime emits `planner-mode-resolved`; browser debug/Inspector/Support Bundle now expose configured planner mode, effective planner mode, and resolver reason. Docs moved native-tools caveats from host discipline into the runtime provider matrix. Verification: provider capability unit, native-tools/runtime concern tests, browser smoke/lint/build, root build/dist/test, and diff check. | 2026-04-29 |
| AGRUN-212a Amendment 2M Required-completion Veto Circuit Breaker | Added `todoAutopilot.maxRequiredCompletionVetoes` so `requireCompletion: true` TodoState finalize vetoes have their own bounded circuit breaker instead of bypassing `maxVetoes` forever. When a pure-block required-completion veto reaches the cap, the runtime returns a deterministic `todo_run_next` redirect with an explicit note, preserving TodoState audit trail instead of silently allowing finalize with unfinished items. `before-finalize-veto` now emits `requireCompletion`, `vetoCount`, `requiredCompletionVetoCount`, `maxVetoes`, `maxRequiredCompletionVetoes`, `limitReached`, `remainingCount`, `activeLabel`, `decision`, and `recommendedEscapeAction`. Verification: TodoState autopilot unit test, build, dist check, and diff check. | 2026-04-29 |
| AGRUN-213p Browser Inspector Disabled Actions Summary | Added an independent Browser Inspector Summary card for the effective per-run disabled actions surface. The card reads from the same `runtime.runOptions.disabledActions` debug SSOT used by Debug Report and Support Bundle, shows the effective count, renders each blocked action as a stable token, and displays `none` for runs without disabled actions. Browser smoke coverage now pins Summary disabled-action count and action list. Verification: browser lint, browser smoke, and browser build. | 2026-04-29 |
| AGRUN-213o Browser Inspector Planner Directives | Added browser-example Inspector visibility for effective per-run planner directives. The browser host now builds a serializable `runtime.runOptions` debug snapshot from runtime-level, default-run, and per-call planner directives, including directive mode, disabled actions, and the effective directive list used for the turn. Inspector Summary shows effective directive count/mode; Debug Report emits a `[planner_run_options]` section with all effective lines; Support Bundle includes `plannerRunOptions` for trace handoff. Browser smoke coverage pins Summary/Report/Support Bundle output. Verification: browser lint, browser smoke, and browser build. | 2026-04-29 |
| AGRUN-213n Planner Phase Hooks | Added per-run `plannerDirectives` / `plannerDirectivesMode` to the shared run-options SSOT so both `runtime.run()` and `session.run()` can append or replace runtime-level planner directives without rebuilding the runtime. Added `onInvalidPlannerOutput(rawText, parseError, runState)` before the repair cascade so hosts can return a valid envelope and skip the repair LLM call. Repair-cascade Stage 2 now preserves the original planner system instruction as system-level repair context, then appends the repair-only instruction block. Updated public API and planner architecture docs, including the current planner-selected `finalize` contract. Verification: provider concern tests cover append, replace, invalid-output hook recovery, and repair context preservation. | 2026-04-29 |
| AGRUN-213m Browser Host Turn-Intent Smoke | Wired the browser example host classifier prompt to return normalized `turnIntent.kind` through the existing `threads.intentClassifier` callback, including `kind: "new_task"` + `divergentIntent: true` when the user starts a different goal despite weak overlap. Extended browser debug snapshots with `intentState` so Inspector/live harnesses can verify `intentState.goal`, `turnState.goalAnchorText`, and `contextSnapshot.turnIntent.kind` from the same turn. Added a live stale-anchor regression to `test:live:browser-multiturn` for the email case and hardened the CDP composer submit helper so controlled React textarea writes are confirmed before waiting for assistant output. Verification: browser lint/smoke, live Gemini stale-anchor, and live OpenAI stale-anchor. | 2026-04-29 |
| AGRUN-213l Goal Anchor Turn-Intent Resolver | Honored host/LLM `threads.intentClassifier` turn intent before current-turn goal anchoring. Classifier output may now include `kind: "new_task" \| "follow_up" \| "drill_down" \| "unknown"`; `new_task` is normalized into the router's divergent signal and forces inquiry/turn-state goal anchors to use the current prompt instead of inheriting stale `activeGoal` from weak token overlap. Added regression coverage for the email case (`Top 10 best selling items in 2010 by quantity` → `sales 2010 vs 2011`) plus real session coverage proving host classifier wiring seeds `intentState.goal`, `turnState.goalAnchorText`, and `contextSnapshot.turnIntent.kind` from the same SSOT. Browser Inspector Summary, debug report, and support bundle now expose turn-intent kind for QA. Docs updated in public API and context/continuity model; generated root/browser dist reviewed as release artifacts for this runtime + example UI change. Verification: targeted unit tests, browser lint/smoke/build, and `npm run check`. | 2026-04-29 |
| AGRUN-213k Browser Live Evidence + KB Sync | Added `agrun_docs/live-tests/browser-multiturn-e2e-2026-04-29.md` as the baseline evidence record for the post-merge browser multi-turn E2E. Linked the release checklist to the live evidence doc and mirrored the reusable QA rule into MCP KB: browser/provider/planner/session/Inspector changes should run `npm --prefix examples/browser run test:live:browser-multiturn -- --provider all` with real `.env.local` keys, use `qa_clean=y`, and treat skipped live tests as not-passed. | 2026-04-29 |
| AGRUN-213k Browser Multi-turn QA Hardening | Fixed Browser Inspector success classification so completed runs without errors show `Healthy Run` instead of the misleading default `Provider Issue`. Added `qa_clean=y` browser QA mode to clear chat/runtime storage while preserving provider settings, preventing old IndexedDB/global-memory pollution during live tests. Added `npm --prefix examples/browser run test:live:browser-multiturn -- --provider all`, a headless Chrome/CDP live E2E that auto-starts the browser example with `.env.local`, runs clean-context Gemini/OpenAI multi-turn continuity checks, asserts `plannerMode: "native_tools"`, native tool-call coverage, no provider/action-args errors, and no secret leakage. | 2026-04-29 |
| AGRUN-213k Post-merge Browser Live Release Checklist | After PR #16 merged, synced `main`, cleared stale generated root `dist/` noise (`dist/agrun.js`, `dist/agrun.md`, untracked `dist/Archive.zip`), and re-ran `npm --prefix examples/browser run test:live:browser-multiturn -- --provider all` with `.env.local`. Gemini passed 3-turn continuity with native final/action coverage and `list_agent_skills`; OpenAI passed 2-turn continuity with native final coverage. Added browser live multi-turn E2E to the release checklist for browser/provider/planner/session/Inspector changes. | 2026-04-29 |
| AGRUN-213k Browser Native Tools Debug E2E | Integrated browser-example runtime config debug projection so Inspector Summary, debug report, and Support Bundle expose `plannerMode` plus `nativeToolsFailurePolicy`. Verified the browser example live with `.env.local` auto-seeded Gemini on the default `native_tools` path: the run emitted `planner-native-tool-call` for action and final cycles, executed `list_agent_skills`, and returned `BROWSER_NATIVE_OK`. | 2026-04-29 |
| AGRUN-213k Native Tools Default Switch | Switched the default planner mode to `native_tools` for runtimes without explicit `plannerMode`. Preserved `plannerMode: "envelope"` as the compatibility opt-out and kept `nativeToolsFailurePolicy: "fallback_to_envelope"` as the default failure strategy. Updated public docs, ADR-0003, readiness matrix, generated dist docs, and runtime/native-tools tests to lock the new default and envelope opt-out. | 2026-04-29 |
| AGRUN-213j Native Tools Default Readiness Gate | Added a strict `native-readiness` live suite covering OpenAI/Gemini native action, clarify, finalize, approval, search, and TodoState. Hardened Gemini native finalize with a one-shot runtime-finalizer empty-response retry that emits `runtime-finalize-empty-response-retry` and asks for a non-empty final answer when the provider returns no text/tool output. The live gate passed 12/12 with `.env.local` (`executed=12 skipped=0`), clearing the default-switch gate for AGRUN-213k. | 2026-04-29 |
| AGRUN-213i Native Tools Failure Policy | Formalized native_tools failure semantics. Added `nativeToolsFailurePolicy` to runtime config with default `"fallback_to_envelope"` so existing supported-with-caveats behavior stays compatible; `"hard_fail"` emits `planner-native-tools-failed` and returns the normalized planner/provider error without envelope retry. Concern coverage verifies both default fallback and hard-fail behavior. Release note added to `CHANGELOG.md`. Live hard-fail smoke with `.env.local` OpenAI/Gemini and intentionally invalid models verified `PLANNER_ERROR`, `planner-native-tools-failed`, `provider-error`, and no fallback step while logging only scrubbed fields. Docs updated in public runtime API, feature toggles, planner architecture, native-tools readiness, and live checklist. | 2026-04-29 |
| AGRUN-213h Live Suite Cleanup + Native Failure Policy Deferral | Fixed `test/live.test.mjs` open handles by clearing/unrefing `withScenarioTimeout` timers instead of hard-exiting the process. Re-ran live readiness suites: `npm run test:live:planner` PASS 8/8 and exited cleanly, `npm run test:live:approval` PASS 5/5 and exited cleanly, `npm run test:live:search` PASS 7/7 and exited cleanly. Native failure policy is explicitly deferred: current native_tools -> envelope fallback behavior remains unchanged until a dedicated slice decides fallback vs hard-fail semantics. | 2026-04-29 |
| AGRUN-213g Native Tools Supported-with-Caveats | Added formal live planner coverage for native TodoState: `openai-native-tools-todo` and `gemini-native-tools-todo` now run under `npm run test:live:planner` and assert native `todo_plan` creates a non-empty structured TodoState. Evaluated native_tools readiness after 213f hardening: status moved from experimental/done-with-blockers to supported-with-caveats as an opt-in mode at that time; AGRUN-213k later made it the default. Real host proxy coverage is N/A/deferred because no host proxy exists; local forwarding proxy remains the header-sanitization check. Verification: `npm run test:live:planner` printed PASS for 8/8 planner cases, including the new TodoState cases. | 2026-04-29 |
| AGRUN-213f Native Tools Readiness Hardening | Hardened the readiness blockers found after 213e. `todo_plan` now has native-mode preflight: native planner outputs must include at least one item with a non-empty label before mutating `runState.todoState`; invalid outputs emit `action-args-invalid` and can self-correct. Plan validation now passes `runtimeConfig` into action preflight so the same gate works inside native/envelope `type:"plan"`. Added `src/runtime/provider-capabilities.js` as SSOT for provider-specific native-plan behavior; Gemini is marked `toolArgsJson`-required and prompt/schema descriptions read that capability instead of duplicating hardcoded rules. Verification: targeted planner-tools/todo/plan-validation/native-tools concern tests, focused OpenAI/Gemini native TodoState live retest, and OpenAI/Gemini local server-auth native proxy retest with provider auth headers stripped. Real host proxy coverage is N/A/deferred until a host proxy exists. | 2026-04-29 |
| AGRUN-213e Native Tools Default Readiness | Added `agrun_docs/native-tools-readiness.md` with an envelope vs native_tools matrix across provider, action, plan, clarify, final, finalize, approval, invalid args, fallback, TodoState, and browser/server-auth surfaces. Initial decision was to defer the default switch until blockers were closed; AGRUN-213k later switched the default after 213j made the readiness gate pass. Native tools support single action / clarify / final / finalize / native `plan`, including flat `execute_skill_tool` fields (`skillName` / `toolName` / `toolArgs`), recursive Gemini `propertyOrdering`, `toolArgsJson`, and safe raw native tool-call args-shape debug. Gemini native plan should be treated as `toolArgsJson`-required: nested `toolArgs` and direct flat fields are not Gemini-live-stable, while `arg_*` only passed via self-correction. Added ADR-0003, live checklist, parser/schema tests for native plan, and a mocked OpenAI native_tools concern covering final/action/plan/clarify/native-failure fallback. Verification: targeted planner-tools tests, native-tools concern, `npm run check`, `npm run test:live:planner`, `npm run test:live:search`, approval live PASS summary, strict `.env.local` live checklist for OpenAI/Gemini native final/action/clarify/finalize (8/8 pass), OpenAI native two-action plan pass including flat/propertyOrdering plan shape, Gemini `toolArgsJson` plan pass in client/local server-auth/non-forced skill-plan modes, and OpenAI/Gemini server-auth local proxy pass. | 2026-04-29 |
| AGRUN-213c Provider Failure Error Clarity | Re-scoped 213c away from provider fallback and landed user-safe provider error normalization. Provider call failures now attach safe details (`provider`, `status`, `code`, `reason`, `retryable`, `message`), emit `provider-error` steps, preserve existing public codes such as `PLANNER_ERROR`, and scrub API keys / bearer tokens / authorization headers from public `message`, `details`, and `cause`. Missing provider config now returns a clearer provider configuration message. Fallback provider config remains deferred. Verification: provider-error unit tests, providers concern test, and `npm run check`. | 2026-04-29 |
| AGRUN-213b + Native Tools Schema Alignment | Landed structured runtime event contract on the existing `onStep(step, snapshot)` sink. Step details now get stable `runId`/`cycle`, action and finalize events carry `durationMs`/`callId`, provider/finalize/planner events expose sanitized usage when available, policy blocks emit `policy-blocked`, and `runState.metrics` accumulates planner/provider call counts, action durations, and token usage. Native OpenAI/Gemini tool schema now prefers `planner.argsSchema`, with `argsExample` only as fallback, so native tools no longer infer required fields differently from 213a validation. Docs updated in `agrun_docs/public-runtime-api.md` and `agrun_docs/action-contract.md`; tests added for runtime event metrics and native schema projection. Verification: targeted unit tests and `npm run check`. | 2026-04-29 |
| AGRUN-213a Slice 1 | Landed planner-visible action args contract hardening. Every planner-visible runtime action now declares `planner.argsSchema` or must explicitly opt out with a reason; the registry enforces the invariant. Single action and plan paths both use `validateActionArgs`, plan path emits `action-args-invalid` with `planIndex`, alias rewrites normalize plan args before execution, and action preflight failures are routed into the same invalid/self-correction shape before tool execution. Added schemas for web/search/read-url/skill/todo/clarification actions and documented the contract in `agrun_docs/action-contract.md`. Verification: targeted unit tests, planner/runtime/research/bundled/read-url concern tests, plus `npm run check`. | 2026-04-29 |
| Dist Docs QA Harness | Fixed zero-context `dist/` review findings by emitting `dist/CHANGELOG.md`, rewriting release-note category links per dist context, cleaning `.DS_Store` from `dist/` at build start, and adding `npm run dist:check` to scan generated Markdown links plus unwanted metadata. `npm run check` now gates build output before running tests. Verification: `npm run check`. | 2026-04-28 |
| Dist Docs Split | Extended the Rollup usage-doc plugin so `npm run build` now emits a short `dist/agrun.md` navigation page plus detailed generated docs under `dist/agrun_docs/`. The plugin keeps manifest-indexed docs as the displayed index, discovers additional Markdown files under `agrun_docs/` so referenced ADR/live-test/archive docs are still packaged, rewrites internal doc links to local `dist/agrun_docs/*` paths, and rewrites source/test/build links to GitHub. Verification: `npm run build`, generated `dist/agrun.md` is 144 lines, `dist/agrun_docs/` contains 54 Markdown files, link QA found no stale bundle anchors or malformed external `/agrun_docs/` URLs, and `npm test` passed. | 2026-04-28 |
| Docs Directory Rename | Renamed root documentation directory from `docs/` to `agrun_docs/`. Updated the docs manifest path, Rollup usage-doc import, docs index generator output, repository README links, internal doc links, source comments, tests, generated `dist/agrun.md`, and checked that external `/docs/` URLs were not accidentally rewritten. Verification: `npm run docs:index`, `npm run build`, and `npm test`. | 2026-04-28 |
| Docs Bundle Design | Added `agrun_docs/docs-bundle-design.md` to capture the proposed redesign for generated `dist/agrun.md`: keep `agrun_docs/manifest.cjs` as SSOT, make `dist/agrun.md` a short task/feature index, emit longer generated files under `dist/agrun_docs/`, and migrate feature docs in phases. This is design-only; no build harness behavior changed. | 2026-04-28 |
| AGRUN-212a Amendment 2P | Added `defaultRunOptions` as a first-class hook installation point so hosts no longer need to wrap both `runtime.run()` and `session.run()` for universal observability/control. `normalizeRuntimeConfig` stores sanitized default run options; new `mergeRunOptions` SSOT composes `onStep`, `onToken`, `onPlannerDecision`, `onToolResult`, `onBeforeFinalize`, merges `disabledActions`, and merges default/per-call `abortSignal`s. Runtime and session entry points both merge defaults before execution; per-call hooks still run and can override rewrite-hook outputs. `agrun_docs/public-runtime-api.md` documents the option and the runtime/session hook boundary. Pinned by `runtime-basic` assertions against `dist/agrun.js` proving default + per-call `onStep` fire for both `runtime.run()` and `session.run()`. Verification: `npm run build`, `node test/concerns/runtime-basic.test.js`, `node test/unit/final-response-prompt.test.js`, and `npm test`. | 2026-04-28 |
| AGRUN-212a Amendment 2O | Fixed residual finalize prompt pollution from disabled citation coverage and documented hook installation boundaries. `buildFinalResponsePrompt` now accepts `{ runtimeConfig }` and skips `Citation coverage warning` injection when `runtimeConfig.citationCoverageGuard` is `false` or `{ enabled: false }`; `executeRuntimeFinalize` passes runtimeConfig through for both initial and repair finalizer prompts. Added `test/unit/final-response-prompt.test.js` proving warning injection remains enabled by default for missing direct-source news targets but disappears when citation coverage is disabled. `agrun_docs/public-runtime-api.md` now explicitly states `runtime.run()` and `session.run()` accept the same run options but are separate entry points; monkey-patching only `runtime.run()` does not affect `session.run()`, so universal hooks must be passed at the actual call site or installed on both. Verification: final-response prompt/quality/citation unit tests, `npm run build`, and `npm test`. | 2026-04-28 |
| AGRUN-212a Amendment 2N | Fixed coverage guard config observability/pass-through from Globe3 ERP defect report. `normalizeRuntimeConfig` now preserves `researchCoverageGuard` and `citationCoverageGuard` so `{ enabled: false }` survives into `session.runtimeConfig` and the existing guard normalizers can honor it instead of silently defaulting to enabled. Added public `runtime.getRuntimeConfig()` returning a cloned snapshot of resolved runtime config for host boot checks and Inspector/debug surfaces; mutating the snapshot does not affect the live runtime. Documented both guard options and the new accessor in `agrun_docs/public-runtime-api.md`. Pinned by runtime-basic concern assertions against `dist/agrun.js`; verification: `npm run build`, `node test/concerns/runtime-basic.test.js`, and `npm test`. | 2026-04-28 |
| AGRUN-212a Amendment 2M | Fixed ERP tool-backed report false citation/research coverage. Root cause: `report` alone was treated as article/news-like intent across research coverage, EvidencePack, final source filtering, and final-response target-source fallback, so internal ERP prompts could turn customer names/currency codes/control words into missing news targets and rewrite final answers into "verified current news report" insufficiency stubs. Added `src/runtime/external-source-intent.js` as SSOT for external source coverage intent: explicit news/headlines/current-events/web-search/source/citation/URL/direct-article verification prompts still activate coverage; pure internal/tool reports do not. Wired the gate into research coverage, citation EvidencePack target extraction/concrete-article mode, final-response source coverage, and final source article filtering. Pinned by ERP regression cases in research coverage, citation coverage, and final-response quality tests; explicit US/MY/SG news tests remain unchanged. Verification: targeted unit tests green, `npm run build` green, `npm test` green. | 2026-04-28 |
| AGRUN-CALLER-ABORT (follow-up) | 100% path coverage. Extended caller cancellation to runSkillLoop and compaction. `runSkillLoop` now reads `callerAbortSignal`, fail-fast at entry, top-of-iteration check between cycles (throws AbortError), and merges signal into `initialInput.signal` so any skill that calls `requestProviderCompletion(context.input, ...)` inherits cancellation without skill-side changes (cloneValue preserves AbortSignal by reference). `action-loop-session-loop.continueActionLoop` got the same top-of-iteration check via new `session.callerAbortSignal` field exposed by `createActionLoopSession`. `session/compaction.js` `summarizeSessionContext` now sets `signal: options.callerAbortSignal` on the request so a Stop click during a slow compaction summarize call also cancels (compaction runs BEFORE runLoop). `session/handle.js` `prepareSessionInput` propagates `callerAbortSignal` into `prepareProviderSessionContext`. New test: `caller-abort: skill-loop branch fail-fast on pre-aborted signal` (covers non-provider input → runSkillLoop branch). Full suite 452/452 green. | 2026-04-28 |
| AGRUN-CALLER-ABORT | Added caller cancellation contract — `session.run(input, { abortSignal })` now honours a standard `AbortSignal`. SSOT helper `src/runtime/abort-signal.js` provides `mergeAbortSignals` (uses `AbortSignal.any` when available, manual fallback otherwise), `createAbortError`, `wrapHookForAbort`. Wiring: `session/handle.js` extracts `runOptions.abortSignal`, fail-fast checks pre-turn, wraps `onStep` / `onToken` / `onPlannerDecision` / `onToolResult` / `onBeforeFinalize` so they become no-ops post-abort, throws `AbortError` post-runLoop if signal aborted (so caller cancellation surfaces as a thrown `AbortError` instead of a "succeeded with failure" result). `runtime/run-loop.js` fail-fast at entry. `action-loop-session.js` attaches caller signal to `request.signal` once at the SSOT — every downstream `requestProviderCompletion(session.request, …)` inherits it via `createRequestOptions` spread, no per-callsite plumbing. Browser providers (`gemini-browser.js`, `openai-browser.js`) now `mergeAbortSignals([request.signal, createTimeoutSignal(...)])` instead of `||` — caller cancellation AND per-request timeout both fire. Documented in `agrun_docs/public-runtime-api.md` § "Caller Cancellation (`abortSignal`)" with Stop button example; auto-bundled into `dist/agrun.md`. Pinned by `test/concerns/caller-abort.test.js` (3 cases: pre-aborted rejects with `AbortError`, mid-stream abort propagates into fetch's `init.signal`, `onStep`/`onToken` suppressed after abort). Full suite 451/451 green. Closes Globe3 ERP integration ticket: Stop button now actually stops the LLM stream and the bill. | 2026-04-28 |
| TodoState Integration Doc | Wrote `agrun_docs/todo-state-integration.md` and bundled it into `dist/agrun.md` via `agrun_docs/manifest.cjs` (`bundle: true`). The doc consolidates the host-facing contract surface that was previously scattered across ADR-0010, source comments, and CHANGELOG entries: 7 config knobs, 5 planner actions, full state shape, persistence/thread semantics, complete `onStep` event table, `inspectTodoState` debug helper, `promptStrings` deep-merge override rules, regex policy override rules, common recipes, and a troubleshooting matrix. Closes the largest DX gap identified in the 2026-04-28 third-party-engineer review — third-party integrators no longer need to read source to wire TodoState. Generated `agrun_docs/README.md` index and rebuilt root bundle (`dist/agrun.md` ToC now lists "TodoState Integration"). | 2026-04-28 |
| AGRUN-212a Amendment 2L | Fixed finalize-veto infinite loop discovered via user Support Bundle (2026-04-28). Original symptom: real research prompt ("Find latest AI browser news") produced 80 cycles + 980 runtime steps, terminalized by `max_steps_continuation` after 75 identical wasted veto cycles. Root cause: when `read_url` failed CORS in browser without proxy, `runState.autoReadStoppedReason = auto_read_limit_reached`, but the `maybeCreateTodoAutopilotVeto` fallback ([todo-autopilot.js:128-132](https://github.com/yapweijun1996/agrun/blob/main/0_development/src/runtime/todo-autopilot.js:128)) returned a pure-block veto with `requireCompletion: true`, bypassing the maxVetoes cap (line 223) and providing no actionable redirect — planner picked finalize → veto blocked → next cycle picked finalize again → loop forever. Fix: detect exhaustion via `runState.autoReadStoppedReason` and redirect veto to `todo_run_next` with an "evidence-constrained" note instead of pure-block. Each redirect advances one TodoState item; plan drains naturally; finalize allowed. SSOT: new `autopilot.verifierNudge`-style template `evidenceExhausted({activeLabel,reason})` in `todo-prompt-strings.js`; helper `readEvidenceExhaustedReason(runState)` in `todo-autopilot.js`. Pinned by 3 new cases in `test/unit/todo-autopilot.test.js` (redirect fires when exhausted, regression-safe when not exhausted, 2-item drain loop terminates) + all 17 pre-existing autopilot tests pass unchanged. **Tier 3 whole-system e2e**: same prompt, same CORS-blocked read_urls — now completes in 10 cycles (vs 80) with real `final_answer` activity and a proper 3-link answer. ADR-0010 amendment 2L. | 2026-04-28 |
| Live e2e observation 2026-04-28 | During Amendment 2K verifier-nudge live e2e on `?qa=verifier-nudge-e2e` with `gemini-3-flash-preview`, the LLM bypassed `todo_plan` entirely on a clearly 3-step prompt ("do three things in order: list capitals, describe each, format markdown table"), going straight to `read_url` and hitting the approval gate. Verifier-nudge code path itself was independently verified via direct browser-runtime API calls (5/5 cases pass: nudge fires for 3+ items without verify keyword, skipped with "Run tests" item, skipped below 3-item threshold, NOT re-fired on terminal_noop, host SSOT override works end-to-end). **Follow-up:** independent of 2K, evaluate strengthening `todoPlanRequired` autopilot veto or `todo_plan` description for small-model adherence to "3+ step prompts → call todo_plan first". Not a regression in 2K — the verifier-nudge fires correctly when `todo_plan` IS used; the gap is upstream prompt-tier compliance for the planner choosing `todo_plan` at all on small models. | 2026-04-28 |
| AGRUN-212a Amendment 2K | Verifier-nudge structural reminder ported from claude-code TodoWriteTool. When `todo_run_next` transitions a 3+ item plan to `completed` and no item label/note matches the verification pattern (`verify` / `test` / `check` / `validate` / `qa` / `smoke` / `regression` / `e2e`), the runtime attaches a `verifierNudge` string to the action result and emits a `todo-plan-verifier-nudge` step so the host can see the reminder fire. Threshold (3 items) mirrors claude-code — trivial 1-2 step plans skip the overhead. Terminal_noop calls do NOT re-fire the nudge: it is a one-shot signal at the transition-to-completed moment, not a perpetual reminder. SSOT pattern: `DEFAULT_VERIFICATION_PATTERN` + `normalizeVerificationPattern` live in `src/runtime/todo-autopilot-rules.js`; nudge text template lives in `src/runtime/todo-prompt-strings.js` `autopilot.verifierNudge` (host overridable via `runtimeConfig.todoAutopilot.{verificationPattern,promptStrings.autopilot.verifierNudge}`). Pinned by 4 new cases in `test/unit/todo-actions.test.js` (10e nudge fires, 10f keyword present skips nudge, 10g below-threshold skips nudge, 10h terminal_noop does not re-fire) plus all 7 pre-existing `todo_run_next` tests still green. Root build green. Live e2e deferred — change is additive; existing `todo_run_next` flows remain byte-stable when no completion happens this turn. | 2026-04-28 |
| AGRUN-212a Amendment 2J | Closed a TodoState terminal-state invariant gap found during code review: `applyTodoAdvance` now rejects mutations when the plan status is `completed` or `abandoned`, so terminal plans cannot be revived into active/pending item states. `todo_run_next` now treats both completed and abandoned plans as terminal no-ops and reports abandoned without rewriting it as completed. Chrome MCP E2E found one browser panel issue: with `showTaskProgress=false`, the compact chip said `Expand task progress` but the effect immediately collapsed it again. `ChatWorkspace` now keeps compact mode as the default without blocking explicit user expansion. Pinned by `test/unit/todo-state.test.js`, `test/unit/todo-actions.test.js`, root build/test, browser lint/build/smoke tests, and Chrome MCP E2E: browser runtime import verified active→completed, completed no-op, abandoned no-op, terminal advance rejection, UI rendered active TodoState as `Step 2/3`, expanded details showed goal/current/todo/progress/next-step, and no blocking console/network errors appeared. Skill Reflection: no global skill update; knowledge is project-specific and QA workflow is already covered by `quality-check-improvement-loop`. | 2026-04-27 |
| Skill Learning Rule | Moved post-task Skill Reflection to global Codex scope under `/Users/yapweijun/.codex/AGENTS.md` and `/Users/yapweijun/.codex/skills/skill-learning/SKILL.md`. Removed the project-local bundled duplicate so agrun's `skills/*/SKILL.md` stays focused on runtime agent instruction packages. | 2026-04-27 |
| AGRUN-212a Amendment 2I | Plan-drift goal-anchor fix. Live e2e on gpt-5-mini and gemini-2.5-flash showed planner LLMs replanning with generic recovery templates ("Recover paused task state", "Re-establish connections", "Execute next work unit") completely off-topic from the user request. Static analysis found planner omitted `goal` on replan and `applyTodoPlan` preserved an empty prior goal — no anchor → LLM training-data fallback. Two-tier fix: (1) prompt tier — `todo_plan` description + planner.guidance now mark `goal` as REQUIRED on every call with REPLAN ANCHOR section listing the three forbidden phrases; (2) runtime backstop — `executeTodoPlanAction` falls back to `runState.observationSummary.prompt` when planner omits goal, emitting observable `todo-plan-goal-anchored` step. Pinned by `test/unit/todo-plan-goal-anchor.test.js` (6 cases) + 6 pre-existing todo test files unchanged. ADR-0010 amendment 2I. | 2026-04-27 |
| AGRUN-212a Amendment 2H | Policy/Mechanism separation: extracted all LLM-facing TodoState strings into `src/runtime/todo-prompt-strings.js` SSOT (autopilot veto observations + ACTIVE TODO PLAN block header/footer/placeholder/templated counts). Added `normalizeTodoPromptStrings` deep-merge with strict type rules (function defaults reject string overrides; empty/garbage values fall back; unknown sections dropped). Wired through `normalizeTodoAutopilotConfig.promptStrings`, `todo-autopilot.js` (9 observations), `todo-state-prompt.js` (block contents), and `action-loop-planner.js` (threads runtimeConfig.todoAutopilot.promptStrings into prompt-block builder). Defaults preserve historical wording byte-for-byte; all 6 pre-existing todo test files pass unchanged. Pinned by `test/unit/todo-prompt-strings.test.js` (8 cases: frozen defaults, override flow, garbage fallback, byte-stable defaults). Live e2e skipped intentionally — change is routing-only; default path was already verified by Amendment 2G live e2e and override path is exhaustively unit-tested across all 5 consumers. Out of scope: `actions/todo-actions.js` description/guidance (≈15 strings) deferred to v2 since they're action-catalog policy, not runtime-veto policy. ADR-0010 amendment 2H. | 2026-04-27 |
| AGRUN-212a Amendment 2AF | Fixed continuation prompt leakage in final/source target resolution after live US/MY/SG QA produced an `April 27` section instead of `United States`. Added reusable `final-source-prompt` resolver so finalization, terminal output normalization, research coverage, citation coverage, and final-response quality all skip host resume prompts and fall back to the real original query / TodoState goal. Pinned by final-source prompt, final-response quality, research coverage, citation coverage tests, root build, and browser lint. | 2026-04-27 |
| AGRUN-212a Amendment 2AE | Added Inspector budget/stop-reason projection for long-run QA. Runtime debug snapshots now carry `longRunContinuation` and `sessionBudget` into browser debug metadata; the Support Bundle includes normalized `stopReason`; Inspector shows `Budget / Stop` so max-step, summarize-limit, direct-tool, and session-budget exits are visible without reading raw JSON. Pinned by browser lint, Inspector debug smoke, and browser build. Live QA pending in a fresh isolated `?qa=` chat. | 2026-04-27 |
| AGRUN-212a Amendment 2AD | Fixed QA live-test contamination root cause. `?qa=<id>` now creates/selects an isolated empty QA chat session for that id instead of only enabling Inspector on the previously active chat. This prevents repeated US/MY/SG live tests from reusing the old active thread and producing `continue_thread · best_match_is_active` / stale TodoState evidence. Also repaired the browser Todo progress smoke expectations to match the current structured state contract: `AgentState` uses `executing`, and `blocked` remains visible instead of collapsing to pending. Pinned by browser lint, browser build, Todo progress smoke, and in-app browser QA isolation check showing no stale evidence, no `continue_thread`, and an empty composer for `?qa=codex-isolation-check`. | 2026-04-27 |
| AGRUN-212a Amendment 2AC | Tightened QA/debug polish after live Inspector review. Concrete article source filtering now rejects newsletter/email-digest pages and citation coverage retry queries ask for generic direct article sources instead of provider-specific hardcoded publishers. Browser Inspector support now has a reusable latest-failed-run selector and support-bundle copy helper so QA can copy the most recent failed/blocked/error run without hand-picking raw debug fields. Pinned by evidence provenance, citation coverage, Inspector smoke, root build, browser build, and in-app browser QA on `?qa=1777284350040` after restarting the local dev server. Browser QA confirmed Inspector and Runtime Steps are visible and source labels no longer show `Source: Publisher` text. Known verification note: `npm --prefix examples/browser run lint` is currently blocked by an existing `test/todo-progress.smoke.ts` `acting` AgentState type mismatch outside this change. | 2026-04-27 |
| AGRUN-212a Amendment 2AB | Added QA-mode Inspector discoverability and tightened continuation finalization after live Inspector QA. Browser URLs with `?qa=...` now enable debug controls and auto-open the right-side Inspector on desktop; the header control is a visible `Inspector` button instead of a hidden bug-only affordance. Inspector runtime steps exposed the next root cause: host continuation prompts (`Resume the paused long-running task...`) were being treated as the finalizer's effective user task, which let target/source guards miss the original US/MY/SG goal and caused `Resume/Do Evidence...` output. Final output prompt selection now skips resume-control prompts across runtime finalize, plan synthesize, and terminal result normalization; resume/control words are not extracted as research targets. Final response cleanup also normalizes link labels such as `[Source: Malay Mail]` to `[Malay Mail]`, emits target-coverage fallback as real Markdown sections, and suppresses `Sources:` when every requested target is marked evidence-insufficient. Pinned by final-response quality, research coverage, fresh-news, citation coverage tests, root build, browser build, and live `?qa=` Inspector checks showing auto-open Inspector, Runtime Steps visible, no Resume/Do pollution, no `Source:` link labels, and three clear country sections. | 2026-04-27 |
| AGRUN-212a Amendment 2AA | Tightened direct-source backend and news citation source-kind policy. Browser web-search `auto` now prefers configured SearXNG direct publisher URLs before Gemini grounding fallback; Gemini grounding redirect chunks are resolved with a bounded no-body `HEAD` request before entering evidence. Article-like news/report prompts now reject Q&A/forum/social/wiki/profile/resume-style pages even when they have specific-looking URLs, preventing sources such as Zhihu question pages from being treated as concrete news evidence. Pinned by `test/unit/gemini-grounding-redirect.test.js`, `examples/browser/test/search-backend.smoke.ts`, evidence provenance, citation coverage, root build, browser build, and in-app browser QA: scoped assistant answer had US/MY/SG sections and no provider redirects, no generic `Source: Web Search`, no Zhihu/resume/Q&A/forum links; `Sources:` was omitted when direct country source coverage was still insufficient. | 2026-04-27 |
| AGRUN-212a Amendment 2Z | Added concrete article evidence guard after final live QA still surfaced unrelated canonical citations such as resume/Zhihu links. News/report/latest prompts now treat source selection as article-like: generic news homepages, topic/search/category pages, broad headline portals, provider redirects, and unrelated source pages are excluded from final `Sources:`. Multi-target source selection now only appends target-matching concrete article/source URLs; if none are available, the final answer omits `Sources:` rather than showing fake citations. Direct-final output now also passes through the same final-response structure/source normalizer so no terminal path can leak model-written `Sources:` blocks. Pinned by `test/unit/evidence-provenance.test.js`, `test/unit/final-response-quality.test.js`, citation coverage, root build, browser build, and in-app browser QA: no pending text, no duplicate target sections, no provider redirects, no generic source label, no resume/Zhihu links; no `Sources:` shown when only insufficient/non-concrete evidence was available. | 2026-04-27 |
| AGRUN-212a Amendment 2Y | Added a final-response quality harness after live US/MY/SG QA showed confusing prose even with valid citations: repeated country sections, `(Pending search results...)` placeholders, source-list boilerplate, and resume-triggered source drift. `final-response-quality` now detects empty final text, placeholder artifacts, duplicate requested-target headings, bare/inline unsupported source labels, and normalizes multi-target output to one section per requested target. Planner-final failures are routed through runtime finalization; runtime finalization retries synthesis once against the same evidence; plan-synthesize output gets the same structural normalizer. Finalizer/source selection now uses the effective original task prompt (`observationSummary`, `originalQuery`, `todoState.goal`, thread anchor) instead of a host auto-resume prompt. Pinned by `test/unit/final-response-quality.test.js`, evidence provenance, root build, browser build, and in-app browser QA showing no Gemini empty error, no pending placeholders, no provider redirects, no generic `Source: Web Search`, and one section each for United States/Malaysia/Singapore. Residual: news-report usefulness still depends on selecting/reading concrete article evidence rather than generic news landing pages. | 2026-04-27 |
| AGRUN-212a Amendment 2X | Fixed Gemini empty streaming responses after live US/MY/SG QA stopped at Step 3/4 with `Gemini response did not include text output or function calls`. The previous semantic retry only covered non-streaming `generateText`; runtime finalization uses streaming when `onToken` is active, so empty `streamText` responses still failed. `requestGeminiContentStreaming` now performs the same one-shot semantic retry: if the first stream completes with no text and no tool calls, it retries with an explicit non-empty-response instruction and only forwards retry tokens when recovered. Pinned by expanded `test/unit/gemini-empty-retry.test.js`, existing `test/unit/gemini-streaming.test.js`, root build, and browser build. | 2026-04-27 |
| AGRUN-212a Amendment 2W | Follow-up live QA found two remaining citation-scrub gaps: terminal source selection could lose the user prompt when `runState` prompt fields were empty, causing target-aware source selection to fall back to global results; and model output could write unsupported inline source blocks such as `Sources Malay Mail...` / `Source: planned tool results` that did not match the previous `Sources:` stripper. Runtime terminal source collection now falls back to `request.prompt`, runtime source scrubber removes inline unsupported source blocks and generic planned-tool labels, and browser display applies the same unsupported inline source stripping for persisted/streamed text. Verification passed for evidence provenance, browser citation display smoke, root build, browser build. Live browser retest no longer showed bad unrelated source text, but Gemini returned an empty response before final answer, so a full end-to-end news report pass remains blocked by provider reliability. | 2026-04-27 |
| AGRUN-212a Amendment 2V | Tightened direct-source QA after US/MY/SG browser tests still produced confusing citations. Browser search `auto` now prefers configured SearXNG direct-URL results before Gemini grounding fallback; Gemini grounding redirect URLs are safely resolved with a no-body `HEAD` redirect follow before normalization. Final source handling now removes generic labels such as `Source: Web Search`, augments thread-scoped provenance with current-run direct research URLs, and picks canonical `Sources:` target-aware so multi-country reports prefer one direct source per requested target. If multi-target matching fails, it now returns no canonical source instead of falling back to unrelated global top results such as resume/help pages. Pinned by Gemini redirect, citation coverage, research coverage, evidence provenance, browser search backend smoke, root build, browser build, and clean `git diff --check`. Live QA: no redirect URLs, no technical-error wording, clickable sources when matched; residual quality depends on search result freshness and ranking. | 2026-04-27 |
| AGRUN-212a Amendment 2U | Added citation/source coverage harness after US/MY/SG QA proved that target search coverage alone was insufficient. `citation-source-coverage` now checks multi-target research answers for direct, user-verifiable source URLs per target before finalize, vetoes finalize to search missing target sources, and excludes provider grounding redirect URLs from canonical `Sources:`. Finalizer prompt now receives a citation coverage warning and must mark targets evidence-insufficient instead of writing concrete claims without direct sources. Live browser QA confirmed no redirect URLs, no fake `Sources:`, no technical-error wording, and no internal pause/resume leak; when only provider redirects were available, the final answer correctly refused concrete news claims. Residual: product-quality news reports require a search backend that returns direct source URLs, not only provider grounding redirects. | 2026-04-27 |
| AGRUN-212a Amendment 2T | Added multi-target research coverage guard after citation QA found a wrong finalization path: the agent searched only Singapore, failed to read a grounding redirect, then finalized with misleading "technical error" statuses for Malaysia/US even though those targets had not been searched. `research-coverage-guard` now extracts explicit multi-target research/news/report targets from the user prompt, compares them with executed `web_search` queries, and vetoes finalize by issuing a targeted `web_search` for the first missing target. Finalizer guidance also forbids leaking paused/resumed task wording into user-facing answers. This keeps the fix in the runtime harness instead of hardcoding final-answer text. Pinned by `test/unit/research-coverage-guard.test.js`, evidence-provenance/TodoState regressions, root build, browser build, and clean in-app browser coverage/long-run QA tests. Residual: citation/source relevance still needs a guard because provider redirect URLs may not cover every country section. | 2026-04-27 |
| AGRUN-212a Amendment 2S | Fixed citation UX after live review. Final answers may no longer show `Sources:` with publication names only. The finalizer prompt requires Markdown links, `appendSourcesSection` strips model-written source-name-only blocks and appends canonical URL-backed links from structured evidence, planner-final and runtime-final both use the same normalizer, and the browser display hides legacy persisted `Sources:` blocks that contain no URL. Fresh citation QA confirmed no source-name-only entries and no visible `continue`; residual issue is source relevance because provider grounding redirects can still enter `Sources:` without being clearly tied to the final answer. Pinned by evidence provenance unit test and root/browser builds. | 2026-04-27 |
| AGRUN-212a Amendment 2R | Replaced manual Continue with bounded host-level auto-resume. When a long-running TodoTask hits `continuation_required`, the browser host now auto-resumes the existing agrun session/TodoState without adding a visible or hidden user `continue` bubble. The Todo panel no longer renders a Continue button; it remains a progress/status surface plus Stop/Abandon affordances. Runtime continuation text now says the task will resume automatically, and the browser render layer rewrites legacy persisted assistant prose that said `Say "continue"` so old chats do not keep teaching the wrong action. Fresh US/MY/SG research testing showed auto-resume across multiple runtime-budget pauses; follow-up tightened the transcript so `continuation_required` assistant messages are hidden as runtime status events instead of chat content. Added a per-session auto-resume cap to prevent runaway loops while preserving the agentic default that long tasks proceed step by step without end-user clicks. Pinned by TodoState unit test, root build, browser build, and browser reload check. | 2026-04-27 |
| AGRUN-212a Amendment 2Q | Corrected the long-run resume boundary after UX review. `Continue` is no longer implemented as a visible user chat message. Browser resume now calls the provider turn as a host-level continuation prompt against the existing agrun session, appends only the new assistant result, and keeps the transcript free of synthetic `continue` bubbles. Runtime continuation prose now tells the user to use the task progress resume control instead of typing `continue`; legacy persisted `continue` bubbles are hidden only when they exactly follow a long-run continuation assistant message. Pinned by root build, browser build, and in-app browser reload showing no visible legacy `continue` bubbles. | 2026-04-27 |
| AGRUN-212a Amendment 2P | Retested US/MY/SG today-news long-run flow in the in-app browser. Fixed the progress surface boundary: active/paused/blocked TodoTask now shows a compact bottom `Task progress` bar even when the detailed developer panel is OFF, and desktop collapsed mode uses the same bottom position as mobile instead of disappearing behind `lg:hidden`. The panel can build a snapshot from `todoTask` / assistant debug `runtime.todoTask` when `todoState` is missing after reload. Runtime final synthesis now tokenizes source labels (`planner_finalize`, etc.) so the last synthesis/report item can complete via TodoState before final answer generation. Live result: reload restored `Step 2/4 · Continue`; manual Continue advanced to `Step 3/4`; second Continue finalized and hid the bar. Residual: final answer citation quality is still weak and should be handled as a separate evidence-grounding task. Pinned by TodoState/autopilot/action-progress unit tests, root build, browser build, and in-app browser retest. | 2026-04-27 |
| AGRUN-212a Amendment 2O | Wired browser Todo panel paused cancellation to the runtime cancel contract. The panel now distinguishes active-run `Stop` (AbortController only while executing) from idle `Abandon` (visible when `todoTask.canCancel === true`). `cancelActiveTodoTask` opens the agrun session, applies `applyTodoCancel`, saves the active thread's TodoState through the session store CAS path, and projects a fresh `todoTask` snapshot back into the browser session. No host auto-continue, no UI-only TodoState mutation, and no assistant prose checklist. Pinned by browser build and TodoState/action/lifecycle unit tests. | 2026-04-27 |
| AGRUN-212a Amendment 2N | Added explicit Todo cancellation contract. `applyTodoCancel` is now the single state-machine path for abandoning TodoState: unfinished items become `abandoned`, completed items remain `done`, `activeItemId` clears, plan status becomes `abandoned`, already-abandoned plans are idempotent, and completed plans reject cancellation to avoid rewriting history. `todo_cancel` is registered as a tier-0 planner action so host/planner cancellation can flow through the runtime instead of UI-only mutation. Pinned by `test/unit/todo-state.test.js`, `test/unit/todo-actions.test.js`, and root build. | 2026-04-27 |
| AGRUN-212a Amendment 2G | Added `requireTodoPlanBeforeTools` guard for complex TodoState-shaped turns. When `todoAutopilot.enabled` is true and a task looks like long-running research/news/report/audit/review/compare/debug/investigation/implementation work, the action loop now blocks non-planning decisions until a structured TodoState exists. Allowed before plan: `todo_plan`, `todo_inspect`, `ask_clarification`; blocked before plan: `web_search`, `read_url`, `plan`, `final`/`finalize`, and other tools. This converts TodoState from "optional planner advice" into the first-step harness protocol for complex tasks. Pinned by expanded `test/unit/todo-autopilot.test.js`; `npm run build` passed. | 2026-04-27 |
| AGRUN-212a Amendment 2H | Fixed TodoState false progress. `before-finalize` autopilot no longer injects `todo_run_next`, so the runtime cannot mark work done merely because the planner tried to finalize. It now blocks early finalization and tells the planner to continue the actual active work, then advance TodoState only after that work unit is genuinely complete. `lastObservation` is now always present in the planner loop state, and the TodoState prompt footer no longer tells the planner to use `todo_run_next` as a generic continuation. Planner failures before `todo_plan` now create a blocked TodoState so the browser panel can show why planning stopped. Browser demo max steps increased from 16 to 80 because real 7-10 step news research needs more OODAE cycles than the old cap. Gemini non-streaming now retries once on empty text/tool output. Pinned by `test/unit/todo-autopilot.test.js`, `test/unit/todo-state-prompt-budget.test.js`, `test/unit/gemini-empty-retry.test.js`, root build, and browser build. | 2026-04-27 |
| AGRUN-212a Amendment 2I | Added long-run continuation terminal for active TodoState. When an action loop reaches `maxSteps` while a structured TodoState still has an active/pending item, the runtime no longer returns `MAX_STEPS_EXCEEDED` as a hard failure. It emits `kind: "continuation_required"`, preserves the active TodoState, records `runState.longRunContinuation`, and tells the user/host to continue from the current step. Non-TodoState loops still fail on max steps. Pinned by `test/unit/action-loop-continuation.test.js`. | 2026-04-27 |
| AGRUN-212a Amendment 2J | Browser host now consumes the runtime continuation terminal without host auto-continue. `ChatWorkspace` detects assistant messages whose runtime debug snapshot has `finalAnswerSource: "continuation_required"` / `terminalizedBy: "max_steps_continuation"`; compatibility fallback handles old persisted continuation messages that only have the pause text. The Todo progress panel shows an explicit `Continue` button in expanded and collapsed states for the same signal, but the host does not auto-submit `continue`. The earlier auto-continue experiment was rejected after live UX review and Open WebUI comparison because hidden user-message submission makes progress ownership unclear. Browser host also injects the dynamic current host date into the system prompt so today/latest news tasks do not depend on provider-internal date assumptions. Docs now capture the Open WebUI lesson: task IDs, event-driven status, cancellation, reload recovery, and queue semantics are useful; automatic continuation is not. Pinned by root `npm test`, root `npm run build`, and browser `npm run build`. | 2026-04-27 |
| AGRUN-212a Amendment 2K | Added Todo task lifecycle projection. `src/runtime/todo-task-lifecycle.js` projects structured `runState.todoState` into a host-facing `todoTask` snapshot with stable `taskId`, lifecycle status (`running` / `paused` / `blocked` / `completed` / `abandoned`), progress counts, active item metadata, and explicit affordances (`canContinue`, `canCancel`). Runtime step snapshots now emit `todoTask` beside `todoState`; this is read-only and does not auto-advance or auto-submit `continue`. Added `agrun_docs/todo-task-lifecycle.md` and ADR-0010 amendment. Pinned by `test/unit/todo-task-lifecycle.test.js`. | 2026-04-27 |
| AGRUN-212a Amendment 2L | Wired browser Todo panel to `todoTask` affordances. Runtime step/result `todoTask` now flows through `agent.ts` → `chat-turns.ts` → `ChatSession` → `TodoProgressPanel`. The panel's Continue button now prefers `todoTask.canContinue` and keeps the old assistant-debug fallback only for legacy persisted messages. This keeps continuation explicit and UI-owned without host auto-submit. Pinned by root/browser builds. | 2026-04-27 |
| AGRUN-212a Amendment 2M | Wired `todoTask.canCancel` to active-run stop UI. The Todo panel now shows a `Stop` affordance only while the agent is actively running and the host has the existing abort path available; it calls the same AbortController-backed `stopGeneration` used by the composer. Paused TodoState abandonment is intentionally not implemented here because that needs a runtime mutation contract, not a UI-only field edit. | 2026-04-27 |
| AGRUN-212a Amendment 2F | TodoState ownership corrected after live UX review. Complex-task progress is now planner-owned: the agent must call `todo_plan` with task-specific items generated from the actual user request. Runtime generic 3-step autostart is retired from the default path; placeholder injection is opt-in only via `{ enabled: true, autostart: true }` and is blocked from search/read/finalize until replaced by a real `todo_plan`. Planner-generated lists are capped by `todoAutopilot.maxItems` (default `10`) to keep the host progress panel usable for long research. Pinned by `test/unit/todo-actions.test.js`, `test/unit/todo-autostart.test.js`, `test/unit/todo-autopilot.test.js`, and `test/unit/todo-state-prompt-budget.test.js`; full `npm test` and `npm run build` passed. | 2026-04-26 |
| AGRUN-212a Amendment 2E | Two structural bug fixes from 2026-04-26 live test follow-up. (1) `todo-autostart` placeholder injection is now strictly opt-in via `{ enabled: true, autostart: true }` — the legacy shorthand `enabled: true` no longer fabricates a "Planning task breakdown" placeholder. The planner-prompt already teaches the model to call `todo_plan` with task-specific items; auto-injection was 补丁思维 (treat-the-symptom of "no plan yet" rather than fixing planner prompt coverage). (2) `hydrateRunStateWithThread` now resets `runState.todoState` to `null` when the incoming thread record carries a terminal `status: "completed"` or `"abandoned"` — fixing the carry-over bug where every subsequent turn in the same thread inherited the prior plan's "5/5 done" panel even when the new turn was a different topic with no fresh plan. Active plans still carry over (legitimate cross-turn continuation). Pinned by `test/unit/todo-autostart.test.js` (4 cases), `test/unit/todo-autopilot.test.js` (autostart-shape assertion), `test/unit/thread-hydration.test.js` test 16 (active vs completed vs abandoned vs null hydration). ADR-0010 amendment 2E documents the rationale. | 2026-04-26 |
| AGRUN-212a Amendment 2D | Per-call provider timeout is now plan-aware. Live test (2026-04-26 OpenAI gpt-5-mini) ran `todo_plan` + 5 `todo_run_next` cleanly inside one `session.run()` and persisted plan v=11 status=completed, but the finalize call composing a 5-section comparison table tripped the 60 s `AbortSignal.timeout` default and surfaced as red "Request failed: signal timed out". Fix is structural: new `src/runtime/provider-timeout.js` derives a per-call deadline from `runState.todoState` — 120 s for planner cycles, 180 s for finalize, `null` (provider default) when no plan is in flight. Wired via `provider.js::createRequestOptions` (now honors `overrides.timeoutMs`) and the three planner call sites + the finalize call site. Explicit `request.timeoutMs` always wins (host opt-out). UI half: `ChatWorkspaceComposer` classifies `signal timed out` / `TimeoutError` / `AbortError` as an amber "Response took too long — plan progress was persisted" banner, not red "Request failed". 8-case unit test at `test/unit/provider-timeout.test.js`. ADR-0010 amendment 2D documents the rationale. | 2026-04-26 |
| AGRUN-212a Follow-up | TodoState now auto-starts and auto-continues for complex / long-running planner tasks. `todo_run_next` is a tier-0 harness action that completes the active item, promotes the next pending item, and completes the plan when no unfinished item remains. Todo autopilot can now execute that action from the before-finalize veto path, so the runtime no longer depends on the planner merely reading a hint. Approval resume now preserves TodoState through gated `web_search` / `read_url` actions, including the browser approval view-model sanitizer. Browser live tests: student learning task completed to `Task progress 4/4`; real research task with sources completed to `Task progress 3/3`. | 2026-04-26 |
| AGRUN-212a Incremental Progress Polish | Fixed reviewed `0/3 → 3/3` jump. Browser panel now labels active structured plans as `Step n/total` while work is underway, so an active first item no longer looks unstarted. Runtime now has an opt-in `todo-action-progress` bridge: successful `web_search` / `read_url` actions can advance compatible active TodoState items via the same `todo_run_next` transition used by autopilot, preserving TodoState invariants instead of mutating UI-only state. | 2026-04-26 |
| AGRUN-212a Autostart + Streaming Bridge | Fixed the live long-research gap where the final answer completed but the UI never showed the Todo list until too late. Long / research-shaped turns now autostart a structured TodoState at cycle start (`Search for relevant evidence` → `Review and select the strongest sources` → `Synthesize the final answer`). Step snapshots now carry `todoState`, so the browser panel can show `Step 1/3` while the run is executing. Budget-breach finalization also goes through the same before-finalize guard, so it cannot bypass unfinished TodoState. Live test: in-app browser showed `Step 1/3`, advanced to `Step 3/3`, finished as structured `completed 3/3`, and the assistant answer did not include internal todo/progress text. | 2026-04-26 |
| AGRUN-212a Internal Progress Boundary | TodoState is now treated as internal harness context, not assistant answer content. Planner/finalizer guidance tells the model not to emit `TodoState Progress`, progress trackers, or todo checklists in final prose; the host `Task progress` UI is the user-facing surface. A final-output guard strips TodoState-derived progress sections when a structured TodoState exists. | 2026-04-26 |
| AGRUN-210 | Self-improvement harness MVP — closed across 5 phases. 5 typed records (`ImprovementSignal` / `Hypothesis` / `ImprovementPlan` / `VerificationResult` / `ReflectionResult`), 5-layer non-bypassable lifecycle gate (`finalizeImprovement` + 3 typed errors), 4 reflection rules from AGRUN-118 lessons (concurrency-leak / missing-import / orphan-declaration / mixed-concerns), 3 dogfood fixtures (AGRUN-201, AGRUN-208, synthetic-high), long-running smoke covers recovery + multi-rule composition. ADR-0011 locks scope. Persistence deliberately excluded — host owns plan storage. | 2026-04-26 |
| AGRUN-118 | Test suite split — closed across 19 phases. `test/smoke.test.js` reduced from 6230 → 233 lines (96%); the file is now a thin orchestrator that requires 16 concerns under `test/concerns/` and runs them in serial after the unit suite. Full phase-by-phase narrative in [agrun_docs/archive/agrun-118-phase-history.md](./agrun-118-phase-history.md). | 2026-04-26 |
| AGRUN-208 | Shared `withPatchedGlobals(patches, fn)` helper at [test/helpers/global-isolation.js](https://github.com/yapweijun1996/agrun/blob/main/0_development/test/helpers/global-isolation.js). Migrates `store-indexeddb-migration.test.js` off ad-hoc snapshot/restore. `test/helpers/README.md` codifies the "all `globalThis.*` mutations go through the helper" rule. Helper has 7 own scenarios covering install/restore/throw/undefined-patch/mixed-keys/async/validation. | 2026-04-26 |
| AGRUN-207 | Provider server-auth fetch wrappers switched from black-list strip to allow-list (`content-type` + `accept` only) for both OpenAI and Gemini. Future SDK headers carrying credentials default to dropped. Shared `filterHeadersByAllowList` helper. | 2026-04-26 |
| AGRUN-212a Phase F+ | Browser Todo panel wired to structured `thread.todoState` first; prose parser only fires when `todoState === null` / empty items. Reverse-sync from agrun's IndexedDB session record on session activation (`loadActiveThreadTodoState`) covers multi-tab / fresh-device / cleared-cache reopens. Per-turn envelope reader (`readResultTodoState`) preserves cache on `web_search` / streaming noop turns. Both helpers share a single three-state contract (`object`=overwrite, `null`=clear, `undefined`=preserve) and live in `examples/browser/src/runtime/todo-reverse-sync.ts`; smoke at `examples/browser/test/todo-reverse-sync.smoke.ts` (19 assertions) locks the contract end-to-end. Panel header now renders `goal` from structured TodoState. | 2026-04-26 |
| AGRUN-212a | TodoState runtime harness MVP. 5-field schema + 3 planner actions (`todo_plan`/`todo_advance`/`todo_inspect`) + thread-scoped persistence + compaction exemption + windowed prompt injection (active ±2) + browser panel structured-first read with prose fallback. ADR-0010 locks scope. Shipped across Phases A-G; long-running smoke proves create → advance → reopen → resume on the right next item. | 2026-04-26 |
| AGRUN-205 | OpenAI provider now wires host fetch + endpoint into the AI SDK (`baseURL` for client mode, URL-rewriting + Authorization-strip for server mode). Mirrors Gemini provider's auth-mode harness. | 2026-04-26 |
| AGRUN-204 | Library no longer ships a default SearXNG endpoint. `normalizeWebSearchRequest` rejects empty SearXNG endpoint with an actionable error; consumers / docs updated; public surface scrubbed. | 2026-04-26 |
| AGRUN-206 | Session record CAS via `version` token + `SessionVersionConflictError`. `nextRunId` is now async with bounded retry; concurrent tabs no longer emit duplicate run ids. | 2026-04-26 |
| AGRUN-211 | Browser Todo Progress panel for long-running tasks. Adds `todo-progress` parser, right-side/mobile progress panel, browser smoke coverage, and live test record for long research continuation. → Superseded as primary path by **AGRUN-212a Phase F+** (structured TodoState wins; this prose parser remains the fallback). | 2026-04-26 |
| AGRUN-203 | IndexedDB schema migration is now descriptor-driven (`SCHEMA` table + `ensureSchema`); v→v upgrades retro-fit any missing index instead of skipping when the store already exists. Adds fake-indexeddb regression suite. | 2026-04-26 |
| AGRUN-202 | Durable run identity harness (`src/runtime/run-identity.js`) persists `lastRunSequence` on the session record. Reopening a session resumes the counter instead of reusing `run-1`. | 2026-04-26 |
| AGRUN-201 | Turn ordering harness (`src/runtime/turn-ordering.js`) replaces lexical `turnId` compare in thread-provenance and evidence-scope filters. Fixes `run-10 < run-2` data loss. | 2026-04-26 |
| AGRUN-146 | Goal drift detector with pluggable similarity hook and browser smoke. | 2026-04-22 |
| AGRUN-145 | Thread-aware compaction slices A-E. Follow-up findings tracked as AGRUN-201/202/203/206. | 2026-04-22 |
| AGRUN-144 | Topic router, thread-scoped runtime, intent classifier, browser thread UI. | 2026-04-22 |
| AGRUN-143 | Evidence provenance and finalizer scoping MVP. | 2026-04-22 |
| AGRUN-142 | Goal anchor injection every cycle. | 2026-04-22 |
| AGRUN-141 | Loop convergence via composite budget and action fingerprint. | 2026-04-22 |
